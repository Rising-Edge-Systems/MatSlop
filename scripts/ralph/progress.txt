# Ralph Progress Log
Started: Sat Apr 11 07:38:25 PM EDT 2026

## Codebase Patterns
- Octave `help()` function form returns '' when texinfo filter fails; use `evalc('help <name>')` (command form) for help text, with `get_help_text` as fallback
- Help panel IPC: buildHelpFetchCommand wraps output in `__MSLP_HELP_BEGIN__:<name>\n...__MSLP_HELP_END__` delimiters so extractHelpBody can slice it cleanly
- Unsafe user input into Octave commands: strip to `[A-Za-z0-9_.]` (see buildHelpFetchCommand)
- Test Octave directly at /tmp/octave-root/octave-cli-wrap when validating Octave command snippets
- Existing lint errors (6, all pre-existing: 5x react-hooks/exhaustive-deps rule missing, 1x prefer-const) are NOT from current work — do not try to fix unrelated to story

---

## 2026-04-11 - US-T01
- Fixed Help panel rendering "No help found" for `doc sin` / `help sin`
- Root cause: `disp(help('sin'))` returns empty when Octave's texinfo filter fails (Text::Unidecode missing etc.). The command form `help sin` prints a warning + raw texinfo to stdout.
- Changed buildHelpFetchCommand to use `evalc('help <name>')` with `get_help_text` fallback, inside try/catch that emits `error: <msg>` as the body on failure
- Files changed:
  - src/renderer/editor/helpDoc.ts (buildHelpFetchCommand rewritten)
  - tests/unit/help-doc.test.ts (updated cmd assertions; added fixture test for real Octave 8.4 output + error body)
- Tests: 582 passed / 0 failed. Typecheck clean.
- **Learnings for future iterations:**
  - Verify Octave command snippets against a real Octave install before shipping — function-form vs command-form of `help` behave very differently
  - The Help panel renders `error` state verbatim already; the fix was upstream (always produce a non-empty body in the output stream)
  - Fixture tests should use REAL captured output (including warning lines) not hand-crafted ideal output
  - Pre-existing lint errors in the tree: 5x react-hooks/exhaustive-deps (plugin not loaded) + 1x prefer-const. Ignore when reviewing new changes.
---
