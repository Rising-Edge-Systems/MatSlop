# Ralph Progress Log
Started: 2026-04-14

## Codebase Patterns
- vitest v4 removed `environmentMatchGlobs` — use `// @vitest-environment jsdom` docblock in .test.tsx files
- Typecheck: `npx tsc --noEmit`; Tests: `npx vitest run`; Lint: `npm run lint`
- @testing-library/react, @testing-library/dom, jsdom already installed as devDependencies
- The integration test publish-html-integration has a pre-existing Windows path failure — ignore it
- Use `npx electron --remote-debugging-port=9222 dist/main/index.js` for hands-on testing via CDP
- Use `node scripts/cdp-eval.cjs "expression"` to evaluate JS in the running app
- Use `node scripts/cdp-test.cjs` for the 27-test automated test suite
- CSS variables: use `--accent-color` not `--color-accent` (the latter is undefined)
- LiveScriptEditor uses `useOctaveStatus()` context hook, not the `engineStatus` prop (rc-dock stale prop issue)
- For live scripts, F5 dispatches `matslop:runAllCells` custom event; LiveScriptEditor listens for it
- Cell editor height is set on the `.ls-code-cell-editor` wrapper via JS, not on Monaco DOM node directly
- `.editor-content > section > .monaco-editor` scoping prevents CSS from affecting live script cell editors
---

## Session Summary (2026-04-14 to 2026-04-15)

### Ralph Refactor (US-R01 through US-R26) — All 26 stories completed
- EditorPanel.tsx decomposed from 1,098 lines to 237 lines
- 8 focused hooks extracted: useTabReducer, useSessionPersistence, useFileOperations, useScriptExecution, useEditorShortcuts, useDragDrop, useMenuActions, useDebugIntegration
- Welcome tab removed entirely
- CSS extracted into editor.css and livescript.css
- 12 new test files, 80+ test cases, 800+ total tests passing

### Hands-On Bug Fixes
- Fixed live script cell sizing (wrapper-based approach, CSS scoping)
- Fixed F5 on live scripts routing to Run All Cells
- Fixed engine status stale-prop via useOctaveStatus() context hook
- Fixed --color-accent → --accent-color CSS variable bug (pre-existing)
- Fixed source() wrapper in command window display
- Fixed temp filename display in command window
- Improved add-cell button visibility (30% opacity → full on hover)
- Fixed keyboard shortcut capture phase (Ctrl+W works with Monaco focus)
- Externalized electron-updater in vite config
- Added new file type dropdown (Script .m / Live Script .mls)
- Restored accidentally deleted command window CSS

### CDP Test Suite
- 27 automated tests covering: panels, tab creation, live script rendering, cell height, engine status, code execution, plots, markdown cells, F5 routing, workspace variables
---
