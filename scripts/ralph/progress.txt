# Ralph Progress Log
Started: Fri Apr 10 04:09:20 AM EDT 2026
---

## Codebase Patterns
- Layout uses Allotment panes; panel sizes persisted via window.matslop.layoutSet
- To drive engine-status-dependent UI in e2e without a real Octave process, use `window.__matslopSimulateEngineStatus('busy'|'ready'|'disconnected')` (App.tsx test hook). Pairs with `__matslopSimulatePaused` for flows that need both a busy engine AND a paused debugger state.
- `OctaveProcessManager.pauseForDebug()` (US-020) = SIGINT while busy. Relies on `debug_on_interrupt(true)` set in the init script so the interrupt drops Octave into the debugger at the current line instead of aborting. Prefer this over the older `interrupt()` when the intent is "drop into debugger" — they currently share the SIGINT implementation but differ in semantic + return value.
- Allotment.Pane with `visible={false}` KEEPS children mounted in the DOM. If a data-testid needs to vanish with the pane, conditionally render inside the Pane: `{cond && <Panel/>}`. Also prefer `.dispatchEvent('click')` over `.click()` in Playwright for buttons inside Allotment panes (pattern also used by debug-toolbar.spec.ts).
- Panels use data-testid attributes for Playwright selectors
- Run `npm run build` to verify Vite/electron-vite production build
- Pre-existing typecheck errors in src/main/octaveProcess.ts (env.PATH) are baseline noise
- Integration/unit tests that need bundled Octave fail in dev envs without resources/octave
- Prefer extracting pure helpers into src/renderer/editor/editorTypes.ts and adding vitest unit tests in tests/unit/
- For HTML5 drag/drop E2E: dispatch DragEvents manually via page.evaluate with a shared DataTransfer — Playwright's native drag is flaky
- LiveScript cell data lives in LiveScriptEditor.tsx; serialized via parseLiveScript/serializeLiveScript in editorTypes.ts
- `.ls-cells-grid` is a 2-column CSS grid; each code cell emits paired sibling grid items (code-side at col 1, output-side at col 2). Full-width rows (drop zones, add-cell rows, markdown cells) use `grid-column: 1 / -1`. Hover highlight is driven by a shared `hoveredCellId` React state that applies `.ls-cell-hover` to both halves.
- Per-cell controls (gutter = drag handle + run button/spinner; actions = delete button) live inside `.ls-cell-code-side` (code column), not on the output side. `.ls-cell-actions` uses `opacity: 0` + hover to fade in, so Playwright clicks need `{ force: true }`.
- Live-script vertical scrolling is container-level on `.ls-editor` (overflow-y: auto). Per-cell `.ls-cell-output` uses `max-height: none; overflow-y: visible; overflow-x: auto` so tall outputs expand their grid row (the container scrolls) and wide text scrolls inside the cell. `.ls-cell-output-side` uses `overflow-x: hidden` to prevent wide content from widening its `minmax(0,1fr)` grid track and breaking row alignment.
- Bundled Octave helper scripts live in `resources/octave-scripts/*.m`. When wiring them to the running Octave process, add the directory to `extraResources` in package.json AND addpath it at Octave startup (or copy into the bundled site m/ path).
- Plot JSON schema lives in `src/main/plotSchema.ts`. Use `parsePlotFigure(json)` to validate; unknown series types degrade to `{type:'unknown', octaveType}` so UI can show fallbacks. Emit JSON in Octave with `matslop_export_fig(h)` — it ships its own JSON encoder (don't assume `jsonencode` exists).
- `plotly.js-dist-min` is a runtime dep but has no .d.ts — a minimal `declare module 'plotly.js-dist-min'` in `src/renderer/env.d.ts` covers `newPlot`, `react`, `purge`, `downloadImage`, `Plots.resize`. Use the pure `figureToPlotly(figure)` adapter in `src/renderer/editor/plotlyAdapter.ts` to produce `{data, layout, config}` — it never imports the Plotly runtime, so it's unit-testable in node. Mount via the `PlotRenderer` component, which lazy-imports Plotly via dynamic `import('plotly.js-dist-min')` to keep the 4.7 MB bundle out of the initial renderer chunk.
- Live-script cell figure capture (see `runCellStatements` in `LiveScriptEditor.tsx`) emits TWO markers per plot group: `__MSLP_FIG__:<line>:<pngPath>` (always) and `__MSLP_FIG_JSON_FILE__:<line>:<jsonPath>` (only if `matslop_export_fig` is loaded). Pair them by iteration order per anchor line. The JSON is written via `fopen/fwrite` in Octave (don't try to embed JSON directly in a disp() marker — it has newlines) and read by the renderer via the preload IPC `window.matslop.figuresReadTextFile(path)`.
- To make bundled Octave helper scripts available to the running process: pass `getMatslopScriptsDir()` (octaveConfig.ts) as the second arg to `new OctaveProcessManager(binaryPath, scriptsDir)`; the process's init script addpath()s it before returning from start(). The helper resolves `resources/octave-scripts` in both dev (`app.getAppPath()`) and packaged (`process.resourcesPath`) layouts.
- `LiveScriptCellFigure` carries an optional `plotJson?: string` field. `InlinePlots` prefers `PlotRenderer` (data-testid=`plot-renderer`, wrapped in `data-testid="ls-inline-plot"`) when `parsePlotFigure(plotJson)` succeeds, and falls back to the static PNG `InteractivePlot` otherwise — so a missing/broken `matslop_export_fig` doesn't break the UI.
- Plotly event subscription: after `Plotly.react(el, ...)` resolves, the div gains a jQuery-like `el.on(eventName, handler)` AND `el.emit(eventName, payload)` — the latter is the clean way to fire `plotly_click` / `plotly_doubleclick` / `plotly_clickannotation` from tests without synthesizing real pointer events. Pinned-annotation buckets are split: 2D goes in top-level `layout.annotations` (use `point.xaxis._name` / `point.yaxis._name` for xref/yref so subplots place correctly); 3D goes in `layout[sceneKey].annotations` with sceneKey from `point.data.scene` (defaults to `'scene'`). Set `captureevents: true` on an annotation so `plotly_clickannotation` fires when it's clicked.
- Figure export IPCs: `figures:saveDialog(defaultName)` returns `{filePath, format}` using the extension the user typed/picked; reuse it for any "save a plot" flow instead of rolling a new format chooser. `figures:exportPlot(filePath, data, encoding)` writes base64 PNG (stripping a `data:image/...;base64,` prefix) or UTF-8 SVG/text. Prefer `Plotly.toImage(el, {format, width, height})` over `Plotly.downloadImage` in Electron so the file goes through the IPC/dialog rather than the Chromium download manager.
- **CORRECTION** (supersedes an earlier note): `contextBridge.exposeInMainWorld` produces a frozen proxy in the main world. `window.matslop.fooBar = stub` from `page.evaluate` SILENTLY NO-OPS — you cannot monkey-patch preload bridge methods at runtime. To intercept IPCs in e2e: either invoke the real method and assert its return value, or have the renderer install a dedicated test-only global inside a `useEffect` (e.g. `window.__matslopToggleBreakpoint`) and call THAT from the test.
- Monaco glyph margin / click handling: pointer-based Playwright APIs are flaky on Monaco DOM. Prefer exposing a React test hook via `useEffect` and a companion read-only global (e.g. `window.__matslopBreakpoints`), then drive toggles with `page.evaluate` and poll with `waitForFunction` (React effects may not have flushed by the NEXT evaluate). For the click handler inside Monaco, bind once at mount via `editor.onMouseDown(e => e.target.type === monaco.editor.MouseTargetType.GUTTER_GLYPH_MARGIN)` and read stable refs (`tabsRef`, `activeTabIdRef`, `breakpointsRef`) instead of closing over props — `onMouseDown` is registered ONCE and closures over props go stale.
- Editor tabs expose `data-testid="editor-tab"`, `data-tab-filename=<name>` and (US-014+) `data-tab-id=<EditorTab.id>`. Use `data-tab-id` when the test needs the key used in per-tab renderer state stores.
- Breakpoint state is split into TWO parallel stores (keeps existing toggle path + tests untouched): `BreakpointStore` (lines) in `TabbedEditor.breakpoints` and `BreakpointConditionStore` (line→expr) in `TabbedEditor.breakpointConditions`. Main mirrors this with `debugBreakpoints: Map<string, Set<number>>` and `debugBreakpointConditions: Map<string, Map<number,string>>` — both keyed by `breakpointBucketKey(filePath)`. `reapplyAllBreakpoints()` takes an optional conditions map so restart replays preserve `if '<expr>'`. Right-click the glyph margin to edit a condition; left-click toggles the bp. Use `e.event.rightButton` in Monaco's `onMouseDown` handler to detect right-clicks and call `editor.onContextMenu` to swallow the default context menu on the gutter.
- Per-tab renderer state stores (e.g. `BreakpointStore` in editorTypes.ts): key by `EditorTab.id`, NOT `filePath`, so untitled tabs hold state and two tabs on the same path can diverge. Ship a pure helper (`toggleX`, `clearXForTab`, `getXForTab`) in editorTypes.ts with vitest unit tests, then host the state in TabbedEditor and clean up stale entries in a `useEffect` keyed on `[tabs]` that drops keys no longer in the tab list.
- Plot export failures degrade gracefully via the pure `buildPlotWarning()` helper in `src/renderer/editor/plotWarnings.ts`. Three failure modes: (1) Octave-side error from `matslop_export_fig` — the snapshot script's inner catch emits `__MSLP_FIG_ERR__:<line>:<msg>` (strip newlines via `strrep(.., char(10), ' ')` FIRST); (2) JSON parse failure — caught around `parsePlotFigure` in `InlinePlots`; (3) unknown series types in a good parse — non-fatal advisory. Fatal warnings fall back to the PNG `InteractivePlot`; advisory warnings keep `PlotRenderer` and just show a banner (`data-testid="ls-plot-warning"`). The pairing queue pattern (`errorsByLine: Map<number, string[]>`, consume with shift()) mirrors `jsonPathsByLine` — copy it for any new per-figure marker.
- `window.matslop.openExternal(url)` is a generic http(s)-guarded shell bridge (main handler: `shell:openExternal`) for help-doc links. Reuse it instead of adding per-link IPCs.
- Main-process side-effect helpers that need to talk to Octave should take a `CommandExecutor` (`(cmd: string) => void | Promise<unknown>`) rather than importing the `OctaveProcessManager` singleton directly — lets you unit-test the helper with a recording fake. See `src/main/debugBridge.ts` for the pattern (pure helpers + `BreakpointMapLike` interface + test with a recording exec, native `Map<string, Set<number>>` satisfies the interface for free).
- To replay in-memory state to a freshly-started Octave process, attach a one-shot `status === 'ready'` handler on the `OctaveProcessManager` (set a `replayed` boolean — `status` fires on every busy→ready transition so you must guard). Do NOT `executeCommand(...)` directly after `proc.start()`: there's a latent bug where the init delimiter's no-op resolver nulls `pendingResolve` without triggering `processQueue()`, so commands queued pre-init sit until the next `executeCommand` call arrives.
- Auxiliary BrowserWindows (e.g. detached plot) reuse the main renderer bundle by conditionally mounting a different React tree in `src/renderer/main.tsx` based on `window.location.search`. Spawn from main via `new BrowserWindow(...)`, `win.setMenu(null)`, then `loadFile('../renderer/index.html', { search: 'foo=bar' })` (prod) or `loadURL(ELECTRON_RENDERER_URL + '?foo=bar')` (dev). Pass only a short id in the query string; store the actual payload in a main-process `Map<id, payload>` and expose an IPC like `plot:getDetachedFigure(id)` so the aux window fetches it from its own preload at mount time. Clean up the map entry in the window's `closed` handler and call `mainWindow.focus()` to return focus. For Playwright, use `Promise.all([app.waitForEvent('window'), trigger])` to await the new BrowserWindow without racing.
- Main-process event listeners attached to the `OctaveProcessManager` (`'status'`, `'exit'`, `'error'`, `'paused'`, ...) must be wired in BOTH the `octave:start` AND the `octave:restart` IPC handlers in `src/main/index.ts` — the restart path creates a brand-new manager instance and listeners do NOT carry over. Any new per-process event (e.g. US-016's `'paused'`) needs the same duplicated `.on(...)` block in both handlers; `attachBreakpointReapplier` is the existing prior art.
- Debug/simulation test hooks: for UI tied to main-process events that are hard to synthesize in Playwright (e.g. Octave pause), expose a dedicated `window.__matslopSimulateX(...)` function via a `useEffect` in App.tsx that writes directly to the same React state the real IPC listener would set. Guard cleanup in the effect return. This sidesteps contextBridge's frozen-proxy limitation (you can't monkey-patch preload bridge methods) and keeps the test hook co-located with the real subscription.
- Clicks on elements covered by `.octave-setup-overlay` (shown in dev e2e when no Octave is configured) can't be reached by `locator.click({ force: true })` — Playwright hit-tests the coordinate. Use `locator.dispatchEvent('click')` to fire a synthetic event directly on the target; React's listener fires normally.
- rc-dock (US-024/US-025) lives in `src/renderer/panels/MatslopDockLayout.tsx`: pure `buildDockLayoutFromVisibility(vis)` returns a `LayoutData` tree for any `DockVisibility` set, `buildDefaultDockLayout()` is the first-launch preset. The wrapper component uses `loadTab(id)` + controlled mode (`layout` + `onLayoutChange`) so the host React tree owns panel content; rc-dock's tab-cache is keyed by tab id, so panel state survives layout rebuilds. Stable ids in `DOCK_TAB_IDS` (8 panels). Share `group: 'main'` across top-level panels so US-026 drag-between-docks and US-027 detach-to-OS-window light up for free. Hidden panels are OMITTED from the layout tree entirely (not rendered as empty tabs) so their `data-testid` vanishes from the DOM, matching Allotment's old `visible={false}` + conditional-mount semantics. Import CSS from `rc-dock/dist/rc-dock.css`. ADR: docs/adr/0002-dock-layout-library.md.
- rc-dock does NOT let you stamp custom attributes on its internal `.dock-hbox` / `.dock-vbox` / `.dock-panel` nodes via props. Workaround: add a ref on the outer container and `useLayoutEffect` that walks from a known-id tab's content div up the parent chain until it finds the target box, then `setAttribute('data-testid', ...)` on it. Used in MatslopDockLayout to stamp `editor-column` on the center dock-vbox. Expect a ~30 px height delta vs sibling dock-panel bodies (tab-header bar on panels, none on vbox) — loosen layout-test height tolerances (~60 px).
- To override an existing global keyboard shortcut contextually (e.g. F5 = run normally, but F5 = continue while paused), add a `useEffect` that attaches a window keydown listener in the capture phase (`window.addEventListener('keydown', fn, true)`) only while the override condition holds. Capture-phase handlers run before the bubble-phase shortcut manager so preventDefault/stopPropagation blocks it.
- `WorkspacePanel` refreshes whenever the parent-owned `workspaceRefreshTrigger` counter increments. To re-query `whos` on some new event, bump the counter from App.tsx via `setWorkspaceRefreshTrigger(prev => prev + 1)` in a useEffect keyed on the trigger state — WorkspacePanel's internal effect handles the rest. For e2e verification without a real Octave, App.tsx mirrors the counter onto `window.__matslopWorkspaceRefreshCount` so tests can assert monotonic increments.
- `PanelHeader` accepts an optional `actions` ReactNode slot (between title and collapse button). Use it for inline header controls like the Watches panel refresh button — don't roll a custom header div.
- Watch-style "eval an arbitrary Octave expression safely" pattern: use `buildWatchCommand(expr)` + `parseWatchOutput(raw)` from `src/renderer/editor/watchesStore.ts`. The command wraps `disp(expr)` in a try/catch that printf's a `__MSLP_WATCH_ERR__:<msg>` marker on failure, so a bad expression doesn't leak to stderr or poison the queue. Pair with `formatWatchValue()` to collapse multi-line `disp` output into a single preview line.
- When a `useCallback` handler (e.g. `handleSave`, shortcut-bound actions) must read frequently-changing state (like `pausedLocation`) WITHOUT expanding its dep array (which would re-bind every menu/shortcut listener), mirror that state into a `useRef` inside a separate effect and read `ref.current` inside the callback. Keeps handler identity stable.
- **IMPORTANT:** E2E tests run against the BUILT app in `dist/` (see `tests/e2e/helpers/launch.ts`), NOT the dev server. Edits to `src/renderer/**` do not appear in Playwright tests until you `npm run build`. If a test sees stale DOM (e.g. missing data attributes you just added), rebuild.
- **Test isolation gotcha for electron-store:** ESM hoists `import` statements above top-level code. Any module that creates `new Store()` at module-load time latches onto the production userData dir BEFORE `app.setPath('userData', ...)` runs in `src/main/index.ts`. `src/main/appConfig.ts` now constructs its Store lazily via a Proxy wrapper — do the same in any NEW main-process module that uses electron-store, or the test's `MATSLOP_USER_DATA_DIR` override will be silently ignored and state will leak between runs. (`src/main/octaveConfig.ts` still has the old eager pattern; fix there if you see it cause test flakiness.)
- rc-dock drag/persistence (US-026): the `DockLayout` ref exposes `dockMove(source, target, direction)`, `find(id)`, `saveLayout()`, `loadLayout()`. Use these to drive layout changes in tests — synthesizing drag events on rc-dock's DragDropDiv is too flaky. `MatslopDockLayout` exposes `window.__matslopDockMove`, `__matslopDockGetTabPanelId`, `__matslopDockSaveLayout` test hooks. Persisted dock layout is stored in `LayoutConfig.dockLayout` via the existing `layoutSet`/`layoutGet` IPC; `onDockLayoutChange` filters out rc-dock's internal `'update'`/`'active'` directions so mount-time no-ops don't clobber the store. Visibility toggles rebuild from scratch and therefore clear the saved dock layout (both in `togglePanel` and `resetLayout`).
- rc-dock `fixBoxData` merges a box-with-one-child back into its parent and runs on every controlled-layout reload, so the tree rc-dock actually holds won't match your initial input verbatim. Use `dock.find(id).parent.id` (stable across re-normalizations) when asserting "are these tabs in the same panel" — DOM `closest('.dock-panel')` is unreliable because rc-dock caches inactive tabs in a shared DockPortalManager subtree. Inactive merged siblings have `visibility: hidden`, so use `toHaveCount(1)` instead of `toBeVisible()` for presence assertions on merged tabs.
- Detached aux windows (US-012 plot, US-027 panel) route via query string in `src/renderer/main.tsx`: `?detachedFigureId=...` → DetachedPlot, `?detachedPanelId=...` → DetachedPanel, else App. Main-process IPCs spawn a `BrowserWindow`, track it in a `Map<id, BrowserWindow>`, and the `closed` handler cleans up the map + sends a renderer event (`panel:redocked` or equivalent) + refocuses the main window. The aux React tree may only depend on `window.matslop.*` — it has no access to App.tsx state. To omit a "detached" tab from the dock layout, pass `detachedPanels: Set<string>` to `MatslopDockLayout` — `buildDockLayoutFromVisibility(vis, detached)` drops matching tab ids just like `visibility=false`. Clear the persisted dock layout (`savedDockLayoutRef.current = null`) on detach/redock so rc-dock doesn't re-apply a stale tree that still references the detached tab.
- rc-dock `TabData.title` is typed `string` but accepts a `ReactElement` at runtime — cast via `titleNode as unknown as string`. Lets you attach `onContextMenu`/other React event handlers to tab headers (US-027 "Detach" menu).
- Outside-click dismiss for a custom floating menu: register the global click listener in the BUBBLE phase (not capture) and guard with `menuEl.contains(e.target)`. Capture-phase dismiss runs BEFORE React dispatches synthetic clicks to the menu's own buttons, so the button onClick never fires (menu unmounts first).
- Menu actions that carry a payload (e.g. US-028 `layoutPreset:custom:<name>`, `recentFile:<path>`) encode the payload in the action string. Pair each with a pure builder+parser in a file next to the consumer so the scheme round-trips and can be unit-tested without a live menu. `src/main/appMenu.ts` stays a dumb string join; App.tsx's switch default-case handles parsing.
- View-menu submenus whose entries depend on store state (recentFiles, layoutPresets): extend `buildAppMenu(mainWindow, recentFiles, customPresetNames, ...)` with extra parameters, pass them from both the initial `createWindow` path and `rebuildMenu()`, then call `rebuildMenu()` from every IPC handler that mutates that state.

## 2026-04-10 - US-001 Restructure main layout
- Refactored src/renderer/App.tsx from vertical(top-horizontal+bottom) into 3 outer horizontal columns: FileBrowser | CenterColumn | RightColumn(Workspace+Figure)
- CenterColumn uses a vertical Allotment so Editor sits above CommandWindow+CommandHistory
- Command Window is now horizontally contained within the editor column only
- Added data-testid='editor-column' on the wrapper div and data-testid='editor-panel' on EditorPanel
- Created tests/e2e/layout.spec.ts with 4 bounding-box assertions
- Files changed: src/renderer/App.tsx, src/renderer/panels/EditorPanel.tsx, tests/e2e/layout.spec.ts, scripts/ralph/prd.json
- **Learnings:**
  - Allotment handler signatures: sizes array corresponds to pane order; use sizes[0] for first, sizes[sizes.length-1] for last to survive middle pane hiding
  - `layoutGet` schema has fileBrowserWidth/workspaceWidth/bottomHeight/commandHistoryWidth — repurposed workspaceWidth for right column in new layout
  - electron-vite builds main+preload+renderer; bundle size ~785KB for renderer
---

## 2026-04-10 - US-002 Fix live-script cell drag-and-drop reordering
- Extracted pure `reorderCells<T>(cells, sourceIndex, targetIndex)` helper into src/renderer/editor/editorTypes.ts (handles out-of-range, no-op, and slot-index math)
- LiveScriptEditor now delegates reorder math to reorderCells and no longer mutates inline opacity on the drag-handle (cleaner source-cell visuals via .ls-cell-dragging class only)
- Added data-testid attributes: `ls-cells`, `ls-cell` (+ data-cell-id/index/type), `ls-drop-zone` (+ data-drop-index), `ls-cell-drag-handle`
- CSS: added `.ls-cells-dragging .ls-drop-zone` rule so drop zones grow from 4px → 16px while a drag is in progress, making them actually hittable
- Tests: tests/unit/livescript-reorder.test.ts (8 cases) + new Playwright test in tests/e2e/livescript.spec.ts that dispatches native DragEvents with a shared DataTransfer to move cell[1] above cell[0], then asserts data-cell-id order flipped
- Files changed: src/renderer/editor/editorTypes.ts, src/renderer/editor/LiveScriptEditor.tsx, src/renderer/styles.css, tests/unit/livescript-reorder.test.ts, tests/e2e/livescript.spec.ts, scripts/ralph/prd.json
- **Learnings:**
  - DnD drop zones must be visibly large during an active drag; a 4px line looks tidy but is unhittable
  - `e.currentTarget` inside a drag handler is the element that has the draggable attribute — setting inline style on it only affects the handle, not the whole cell. Prefer className-driven styling for the dragging state.
  - Cell output stays "attached" to its cell automatically because reorderCells moves the same object reference; as long as callers keep _figures/_statementResults on the cell object (not in a parallel map), outputs won't orphan after reorder
  - Playwright HTML5 DnD: use page.evaluate + dispatchEvent('dragstart'|'dragover'|'drop'|'dragend') with `new DataTransfer()` instead of locator.dragTo(); React's synthetic event system picks it up
  - Typecheck baseline noise in octaveProcess.ts env.PATH is ongoing (ignore); unit tests for bundled-Octave still fail without resources/octave in dev
---

## 2026-04-10 - US-003 Apply MATLAB-default layout preset on first launch
- Confirmed the first-launch layout already matches MATLAB defaults via `defaultLayout` in src/main/appConfig.ts (FileBrowser 220px left, Editor+Command Window center, Workspace 280px right). `layoutGet` falls back to this when no stored layout exists, and the 'View → Reset Layout' menu item already routes to the `resetLayout` case in App.tsx which restores defaultVisibility+defaultSizes.
- Added tests/e2e/default-layout.spec.ts with two Playwright tests:
  1. First-launch assertion: verifies FileBrowser sits left of editor column, Workspace sits right of editor column, Editor sits above Command Window inside the center column, and widths are within sane MATLAB-like bounds.
  2. Reset Layout assertion: toggles three panels off via `_testMenuAction`, calls `resetLayout`, then checks bounding boxes match the first-launch snapshot within tolerance.
- Files changed: scripts/ralph/prd.json, tests/e2e/default-layout.spec.ts
- **Learnings for future iterations:**
  - `launchApp()` in tests/e2e/helpers/launch.ts uses an isolated `MATSLOP_USER_DATA_DIR` tmp dir, so every Playwright run naturally starts as a "first launch" — no extra flag needed to test default-layout behavior.
  - `window.matslop._testMenuAction(action)` is a test-only preload bridge (gated on MATSLOP_USER_DATA_DIR) that lets E2E tests drive menu actions directly. Use it for any menu-driven behaviour.
  - Panel visibility toggles + resetLayout are all handled through a single `onMenuAction` switch in App.tsx. Add new menu actions there.
  - MATLAB-default layout is DRY across App.tsx (`defaultVisibility`/`defaultSizes`) AND appConfig.ts (`defaultLayout`). If you change one, update the other or both go out of sync.
---

## 2026-04-10 - US-004 Refactor live script to CSS grid with global code and output tracks
- Refactored `LiveScriptEditor.tsx`: the `.ls-cells` container now has an additional `.ls-cells-grid` class turning it into a 2-track CSS grid (`grid-template-columns: minmax(0,1fr) minmax(0,1fr)`). Each code cell emits two sibling grid items via a React Fragment — `.ls-cell-code-side` (gutter + Monaco editor + delete actions) in column 1, and `.ls-cell-output-col .ls-cell-output-side` in column 2. Outputs are no longer nested inside the code container.
- Markdown cells, drop zones, and add-cell rows span both columns via `grid-column: 1 / -1`. Grid auto-flow fills one row per `code, output` pair and auto-aligns by top using `align-items: stretch`.
- Added a `hoveredCellId` React state: mouse enter/leave on either half sets the id, and both `.ls-cell-code-side` and `.ls-cell-output-side` get an `.ls-cell-hover` class when their id matches — so hovering highlights both halves.
- Removed obsolete `.ls-cell-split` CSS. Reworked `.ls-cell` / `.ls-cell-output-side` borders so the two halves look visually attached (left half: rounded left corners + no right border; right half: rounded right corners + no left border).
- Added `tests/e2e/livescript-grid.spec.ts` with 5 Playwright tests: grid template assertion, paired sibling assertion (+ "not nested" guard), top-alignment via bounding boxes, hover highlight propagation, and a multiplot regression check.
- Files changed: src/renderer/editor/LiveScriptEditor.tsx, src/renderer/styles.css, tests/e2e/livescript-grid.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - `grid-auto-flow: row` fills in sibling declaration order, so using `<Fragment>` (not a wrapper div) is essential — a wrapper div would become a single grid item and break the 2-column alignment.
  - Existing livescript-plots tests target `.ls-cell-output-col` — keep that class name on the output grid item (we add `.ls-cell-output-side` alongside) for backward compat.
  - `data-testid="ls-cell"` must remain on ONE element per cell for the drag reorder test; put it on the code side (for code cells) or the single markdown wrapper (for markdown cells).
  - Hover-highlight across separated siblings is simpler via JS state than CSS `:has()`; React's event system handles enter/leave reliably.
  - Grid cell sizes can use `minmax(0, 1fr)` to prevent Monaco's min-content from blowing out the track width.
---

## 2026-04-10 - US-005 Preserve cell gutter (run button, grip, delete) in grid layout
- No production code changes needed: the US-004 CSS-grid refactor already placed the full gutter (drag handle + run button / spinner) and per-cell actions (delete button) inside every `.ls-cell-code-side` grid item, and `.ls-cells-grid > .ls-add-cell-row { grid-column: 1 / -1 }` already spans add-cell rows across both tracks.
- Added tests/e2e/livescript-gutter.spec.ts with 4 Playwright tests to regression-guard US-005:
  1. Every code cell's run control, draggable drag-handle and delete button are scoped inside its `.ls-cell-code-side` container
  2. Clicking a cell's delete button (force-click to bypass opacity:0 hover state) removes that cell from the grid
  3. `.ls-cells-grid > .ls-add-cell-row` computed gridColumnStart=1 and spans both grid tracks
  4. Drag handle still reorders cells in the grid layout via synthetic HTML5 DragEvents
- Files changed: tests/e2e/livescript-gutter.spec.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Before writing new code for a "preserve X in Y" story, grep the current DOM structure — US-004's grid refactor intentionally kept the gutter+actions scoped inside `.ls-cell-code-side`, so US-005 reduced to writing regression tests.
  - `.ls-cell-actions` uses `opacity: 0` by default (only visible on hover), so Playwright clicks on `.ls-cell-delete-btn` need `{ force: true }` to bypass visibility checks — the element is still in the DOM and functional.
  - `.ls-cells-grid > .ls-add-cell-row, .ls-drop-zone, .ls-cell-md-full { grid-column: 1 / -1 }` is the single rule responsible for full-width rows in the live-script grid. Don't apply `grid-column` on the individual elements; let this selector handle it so new element types inherit the behaviour by adding the class to the selector list.
  - Scoping per-cell controls inside `.ls-cell-code-side` (not the output-side) is the convention — search for controls inside that container when asserting gutter behaviour.
---

## 2026-04-10 - US-006 Continuous output column scrolls with code column
- Changed `.ls-cell-output` in src/renderer/styles.css: `max-height: 300px; overflow: auto` → `max-height: none; overflow-x: auto; overflow-y: visible`. Tall outputs now expand the grid row instead of creating an inner vertical scrollbar, and the existing `.ls-editor` container `overflow-y: auto` scrolls the whole live script.
- Added `overflow-x: hidden; overflow-y: visible` on `.ls-cell-output-side` so wide outputs are clipped inside the grid track (`minmax(0,1fr)`) and cannot widen the track or push neighbor rows out of alignment.
- Added tests/e2e/livescript-scroll.spec.ts with 3 Playwright tests: (1) computed-style assertion that .ls-editor is the scroll container and per-cell `.ls-cell-output` has no max-height/vertical scroll and `.ls-cell-output-side` clamps horizontal overflow; (2) inject 1200px-tall element into first output → output-side offsetHeight ≥1200, editor.scrollHeight grows >500px, no inner vertical scroll, all code/output pair tops still match; (3) inject 5000px-wide element → gridTemplateColumns string unchanged, grid width unchanged within 4px, pair alignment still holds.
- Files changed: src/renderer/styles.css, tests/e2e/livescript-scroll.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - `.ls-editor` already had `overflow-y: auto` from earlier iterations — US-006 was really about removing per-cell scroll boxes so the container-level scroll is the only one.
  - For CSS grid with `minmax(0, 1fr)` tracks, you ALSO need explicit `overflow-x: hidden` on the grid item itself to stop wide intrinsic content from still growing the track. The `minmax(0,…)` only removes the min-content floor from the track algorithm; oversized content visually overflows the item unless clipped.
  - Playwright tests can simulate "tall/wide output" synthetically by injecting a div with fixed pixel dimensions into `[data-testid="ls-cell-output"]` from `page.evaluate` — no need to run real Octave, no need for special fixtures. Clean up the injected node at the end of each test to avoid bleed into the next.
  - When assertions read `getComputedStyle(el).overflowY`, `visible` is the default for no-scroll elements; don't expect `auto`.
  - Baseline vitest failures: `tests/unit/octave-config.test.ts` and bundled-Octave integration tests fail in dev envs (no `resources/octave`) — ignore and move on.
---

## 2026-04-10 - US-007 Extract plot data from Octave as JSON
- Added `src/main/plotSchema.ts` with full TypeScript types (PlotFigure / PlotAxes / PlotSeries) covering all roadmap plot types (plot, plot3, scatter, scatter3, surf, mesh, quiver, quiver3, bar, bar3, contour, contour3, imagesc) and a `parsePlotFigure(input: string | unknown)` validator that accepts either a JSON string or already-parsed object, throws `PlotSchemaError` on malformed input, and downgrades unknown series to `{type: 'unknown', octaveType}` so the UI can surface friendly fallback messages (feeds US-013).
- Added bundled Octave function `resources/octave-scripts/matslop_export_fig.m` that walks a figure handle with `get()`/`findall()` (no optional packages) and emits a JSON string matching the schema. Includes a self-contained `__matslop_to_json__` encoder because Octave 6's `jsonencode` is not reliably available and we want deterministic output from the bundled distro.
- Added `tests/unit/plot-schema.test.ts` with 15 passing vitest cases covering: line, line3, scatter/scatter3, surf/mesh with grid matrices + view angles, quiver3, bar/bar3, contour/contour3, imagesc, multi-axes (subplot), legend + log scales, unknown-type downgrade, default schemaVersion, missing-handle error, malformed-JSON error, mismatched x/y length error, and unknown-field forward-compat.
- Files changed: src/main/plotSchema.ts (new), resources/octave-scripts/matslop_export_fig.m (new), tests/unit/plot-schema.test.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - There was no `resources/` dir at all before this story — created `resources/octave-scripts/` for bundled .m helpers. `package.json`'s electron-builder `extraResources` currently only ships `resources/octave` → if/when US-009 wires `matslop_export_fig` into the running Octave, either add `resources/octave-scripts` to `extraResources` or copy the .m files into the bundled Octave's `share/octave/site/m/` path on install.
  - Octave's `jsonencode` is NOT available on all versions — keep the internal JSON encoder in matslop_export_fig.m rather than depending on it. The encoder handles structs, cells, char, logical, numeric scalar/vector, NaN/Inf (→ null) and escapes quote/backslash/newline/tab.
  - When emitting matrices as JSON from Octave, wrap each row in its own cell array (`cell(1, cols)`) so the JSON encoder produces a true 2D nested array instead of a flat list — use the `__matslop_mat_to_cell__` helper.
  - The TypeScript schema deliberately tolerates unknown extra fields at every level (figure/axes/series) so future Octave emissions can add fields without breaking existing bundles. Unit test "ignores unknown extra fields" regression-guards this.
  - `parsePlotFigure` accepts both a JSON string and an already-parsed unknown — call sites in US-009 can skip an extra JSON.parse if they already have the object.
  - Codebase pattern: when adding a new src/main/*.ts module that is plain TS (no Electron APIs), put vitest unit tests alongside the renderer tests in tests/unit/ using the module's full relative path from tests/unit/ (`../../src/main/...`).
---

## 2026-04-10 - US-008 Integrate Plotly.js as JS 3D plotting library
- Wrote docs/adr/0001-plotly-vs-threejs.md: short ADR comparing Plotly.js vs Three.js. Recommendation = Plotly.js because every roadmap trace type (plot/plot3/scatter/scatter3/surf/mesh/quiver/quiver3/bar/bar3/contour/contour3/imagesc) maps to a built-in Plotly trace, and rotate/zoom/pan/hover/modebar/downloadImage are built-in for free. Bundle size documented: `plotly.js-dist-min@3.5.0` = 4.7 MB minified (one-time installer cost, acceptable for desktop scientific app).
- Added `plotly.js-dist-min` + `@types/plotly.js` to package.json. Declared a minimal `declare module 'plotly.js-dist-min'` in `src/renderer/env.d.ts` (newPlot/react/purge/downloadImage/Plots.resize) so the typechecker doesn't pull in @types/plotly.js's large namespace.
- Added `src/renderer/editor/plotlyAdapter.ts` — a pure function `figureToPlotly(figure: PlotFigure): {data, layout, config}` that maps every roadmap series type: line (+markers), line3→scatter3d, scatter/scatter3, surface/mesh (mesh sets `hidesurface` + `contours.x/y`), quiver→ two traces (lines with null separators + triangle-up markers), quiver3→cone, bar, bar3→scatter3d vertical segments, contour/contour3, image→heatmap. Multi-axes (subplot) → Plotly `xaxis/xaxis2` / `scene/scene2` pairs with `domain` from `axes.position`. Axis limits / log scales / view cameras / legends all mapped. No DOM, no Plotly runtime import → unit-testable in node.
- Added `src/renderer/editor/PlotRenderer.tsx` — ~90-line React component that lazy-imports `plotly.js-dist-min` via dynamic `import()` inside useEffect, calls `Plotly.react(div, data, layout, config)` and `Plotly.purge(div)` on unmount. The lazy import keeps the 4.7 MB bundle out of the initial renderer chunk until a plot is actually rendered (verified: build output still ~789 KB).
- Added `tests/unit/plotly-adapter.test.ts` with 20 passing vitest cases: rgbToCss (×4: rgb/rgba/clamp/undefined) + figureToPlotly (×16: empty, line, line+markers, line3→3D scene+camera, surface, mesh wireframe, quiver 2-trace split, quiver3→cone, bar, contour filled, image→heatmap with colorscale, multi-axes subplots, unknown-series drop, legend visible, no-legend default, axis limits+log scale).
- Files changed: package.json, package-lock.json, docs/adr/0001-plotly-vs-threejs.md (new), src/renderer/env.d.ts, src/renderer/editor/plotlyAdapter.ts (new), src/renderer/editor/PlotRenderer.tsx (new), tests/unit/plotly-adapter.test.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- Quality: `npx tsc --noEmit` → only baseline noise (octaveProcess.ts env.PATH). `npx vitest run` → 20/20 new tests pass (baseline bundled-Octave failures unchanged). `npm run build` → succeeds, renderer bundle unchanged at 789 KB thanks to lazy import.
- **Learnings for future iterations:**
  - `plotly.js-dist-min` does NOT ship .d.ts files; `@types/plotly.js` targets the full `plotly.js` package, not the -dist-min minified entrypoint. Use a small local `declare module 'plotly.js-dist-min'` in `src/renderer/env.d.ts` with only the methods you actually call. Do NOT use TS `declare module` augmentation inside a .tsx file — tsc rejects it ("resolves to an untyped module"); it must live in an ambient .d.ts.
  - Keep the Plotly import out of module scope: use `import('plotly.js-dist-min')` inside `useEffect` so tree-shaking + code-splitting keeps the 4.7 MB off the initial chunk. Before adding PlotRenderer to an existing component in US-009, do the same lazy-import pattern at the call site or rely on this component.
  - Plotly has no native 2D `quiver`; emit line segments with `null` separators (one three-point group per arrow: `[x0, x1, null]`) plus a scatter markers trace for arrowheads. Avoid Plotly's figure-factory (requires the full bundle).
  - Plotly has no native `bar3`; the cleanest degradation is scatter3d "lines" with width 8 from z=0 to z=height per bar. Revisit if US-009 needs prettier 3D bars (mesh3d cuboids are overkill).
  - For 3D scenes, compute `camera.eye` from Octave's `view=[az, el]` degrees via `r * (cos(el)*sin(az), -cos(el)*cos(az), sin(el))` with r≈2.5. Plotly's default r=1.25 is too close for scientific plots with axis cubes.
  - When emitting Plotly subplots, use indexed axis keys `xaxis/xaxis2/...` and `scene/scene2/...`, and per-trace `xaxis: 'x'|'x2'` / `scene: 'scene'|'scene2'` to bind traces to axes. `domain` on xaxis/yaxis handles layout positioning from `PlotAxes.position`.
  - Adapter pattern to preserve: pure `figureToPlotly` + thin React shell. Keeps unit tests fast (no DOM, no 4.7 MB bundle) and makes US-010/011/012 a matter of extending either the adapter (new trace types) or the shell (new button handlers), not both.
---

## 2026-04-10 - US-009 Replace static PNG plots with interactive renderer in live scripts
- Wired the full pipeline matslop_export_fig → JSON file → renderer → PlotRenderer. LiveScriptEditor's `runCellStatements` now also emits a call to `matslop_export_fig(gcf())` for every plot snapshot and writes the returned JSON to `tempdir()/__mslp_<runId>_snap_<N>.json` via fopen/fwrite/fclose, then disp()s a `__MSLP_FIG_JSON_FILE__:<line>:<path>` marker. The renderer pairs these with the existing `__MSLP_FIG__` PNG markers by anchor line + emission order.
- Added preload API `figuresReadTextFile(path)` (IPC `figures:readTextFile`) that reads a UTF-8 file and returns its contents (null on error). Used by LiveScriptEditor to slurp the JSON file into a string attached as `plotJson` on `LiveScriptCellFigure`.
- `InlinePlots` in LiveScriptEditor.tsx now calls `parsePlotFigure(plotJson)` and renders via `PlotRenderer` when parsing succeeds (emits a wrapper `<div data-testid="ls-inline-plot">` around the existing `[data-testid="plot-renderer"]`). When `plotJson` is absent or malformed, it transparently falls back to the static PNG `InteractivePlot`.
- To make `matslop_export_fig` discoverable by the running Octave: `OctaveProcessManager` constructor now accepts an optional `scriptsDir` arg; its init script addpath()s that directory so the function is on the load path. `main/index.ts` wires `getMatslopScriptsDir()` (new helper in `octaveConfig.ts`) into both `new OctaveProcessManager(...)` call sites (start + restart). The helper resolves `resources/octave-scripts` for both dev (app.getAppPath) and packaged (process.resourcesPath) layouts and returns null if the directory is missing (no-op addpath).
- `package.json` `extraResources` now ships `resources/octave-scripts/**/*.m` under `octave-scripts/` so the bundled app includes the helper.
- Rotate/zoom/pan/home: no custom code — Plotly's built-in modebar provides these for free. Verified via the Playwright test which hovers the canvas and asserts the "Reset/Home" modebar button is visible.
- Files changed: src/renderer/editor/editorTypes.ts (+plotJson field), src/renderer/editor/LiveScriptEditor.tsx (snapshot script + parse/pair logic + InlinePlots rendering), src/main/octaveProcess.ts (scriptsDir arg + addpath in init), src/main/octaveConfig.ts (getMatslopScriptsDir helper), src/main/index.ts (pass scriptsDir + figures:readTextFile IPC), src/preload/index.ts (figuresReadTextFile), src/renderer/env.d.ts (type), package.json (extraResources), tests/unit/livescript-interactive-plot.test.ts (new, 3 tests), tests/e2e/livescript-interactive-plots.spec.ts (new, 1 test).
- Quality: `npx tsc --noEmit` → only baseline noise (octaveProcess.ts env.PATH). `npx vitest run tests/unit/livescript-interactive-plot.test.ts` → 3/3 pass. `npx vitest run` → 71 pass, 4 baseline failures (missing bundled Octave). `npm run build` → succeeds; renderer bundle stays small (817 KB) because the 7.1 MB plotly chunk is still lazy-loaded.
- **Learnings for future iterations:**
  - Don't try to embed `matslop_export_fig` JSON directly in a `disp()` marker — the JSON contains newlines and Octave's disp() adds its own, so the marker regex won't survive. Write to a temp file via `fopen/fwrite` and emit only the path.
  - Octave string-embedding gotcha: single-quoted Octave strings need `''` to escape a single quote. Use `path.replace(/\\/g, '/').replace(/'/g, "''")` before interpolating into the addpath source. Forward slashes work on Windows under the `--no-window-system` CLI.
  - `exist('matslop_export_fig', 'file')` returns truthy when the .m file is found on the path, guarding the JSON export so dev environments without bundled helpers don't error out — the PNG fallback still fires.
  - Pair markers by "next-in-queue per line": use a `Map<line, string[]>` + `.shift()` when consuming. Snapshots may share a line (e.g., if someone calls plot twice inside a line continuation), and the queue keeps PNG↔JSON pairing correct.
  - `InlinePlots` must wrap PlotRenderer in a `data-testid="ls-inline-plot"` div so Playwright can assert the interactive pipeline fired. Using only `[data-testid="plot-renderer"]` is enough for click-level assertions but the wrapper lets tests distinguish "interactive path taken" from "PNG fallback taken".
  - Plotly's modebar lazy-mounts on hover; Playwright tests must force `hover({ force: true })` and wait a tick before querying for `[data-title*="Reset"]` or `[data-title*="Home"]`.
  - The existing `livescript-plots.spec.ts` asserts the static `.ls-interactive-plot-image` — it will keep working whenever `matslop_export_fig` is NOT installed on the Octave path (PNG fallback), but on systems where it IS installed it will see PlotRenderer instead. Consider updating those tests if/when bundled Octave + helper scripts ship by default.
---

## 2026-04-10 - US-010 Data cursor / tooltip on plot points
- Plotly hover tooltips already satisfy the "hovering shows coordinates" bullet out of the box; added `hovermode: 'closest'` and a pre-initialized empty `annotations: []` to the layout in `figureToPlotly` so click handlers can append without first creating the array.
- Added pure helpers `formatCoord(n)` and `formatCursorLabel({x,y,z})` in `plotlyAdapter.ts` — 4-sig-digit trimming, exponential for <1e-3 / ≥1e6, em-dash for NaN/null; `<br>` separated multi-line label.
- Updated `PlotRenderer.tsx` to subscribe to Plotly div events via `el.on('plotly_click' | 'plotly_clickannotation' | 'plotly_doubleclick')` after `Plotly.react()` resolves. Click appends a pinned annotation (with arrowhead, bg, border, captureevents=true) either to `layout.annotations` (2D, uses the clicked point's xaxis/yaxis _name for xref/yref) or to `layout[sceneKey].annotations` (3D — scatter3d/surface/mesh3d/cone, sceneKey pulled from `pt.data.scene`). Double-click clears 2D annotations AND iterates `scene`/`sceneN` layout keys to clear per-scene annotation lists. Clicking an existing annotation removes it via the index reported by `plotly_clickannotation`.
- Added `relayout()` to the minimal plotly type declaration in `src/renderer/env.d.ts`.
- Unit tests: 3 new blocks in `tests/unit/plotly-adapter.test.ts` covering hovermode/annotations, `formatCoord` (integers/floats/exponential/NaN), and `formatCursorLabel` (2D vs 3D, null-z). 28 tests pass.
- E2E test: `tests/e2e/livescript-data-cursor.spec.ts` — opens multiplot.mls, asserts `layout.hovermode === 'closest'`, emits a synthetic `plotly_click` via `pd.emit()` on the first trace mid-point, asserts annotations count incremented by 1 and the new annotation text contains "x:" and "y:", then emits `plotly_doubleclick` and asserts annotations cleared to 0. Uses Plotly's internal emit() rather than a real pointer event to avoid coordinate flake.
- Files changed: src/renderer/env.d.ts, src/renderer/editor/plotlyAdapter.ts, src/renderer/editor/PlotRenderer.tsx, tests/unit/plotly-adapter.test.ts, tests/e2e/livescript-data-cursor.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Plotly divs dispatch events with a jQuery-like `el.on(name, handler)` injected on the div after the first `newPlot`/`react` call — NOT on `document`. Tests can trigger the same handlers by calling `pd.emit(name, payload)` directly (also injected).
  - For pinned annotations in 3D plots, the annotation goes in `layout[sceneKey].annotations`, NOT `layout.annotations`. The right sceneKey is on `point.data.scene` (defaults to `'scene'` for the first 3D axes).
  - 2D annotations need `xref`/`yref` matching the clicked axis for correct placement when a figure has subplots — read them from `point.xaxis._name` / `point.yaxis._name`.
  - `annotation.captureevents: true` is required for `plotly_clickannotation` to fire, otherwise clicking a pinned label does nothing.
  - `formatCoord` lives in plotlyAdapter.ts so it's node-testable (no DOM / Plotly runtime import).
---

## 2026-04-10 - US-011 Export current plot view as PNG/SVG
- Added a Plotly export button overlay in `PlotRenderer.tsx` (top-right, `data-testid="plot-export-btn"`) that derives a default filename via `defaultExportFilename(figure)`, opens the existing `figures:saveDialog` IPC, calls `Plotly.toImage(el, {format, width, height})` and writes the result via a new `figures:exportPlot` IPC.
- New pure helpers in `plotlyAdapter.ts`: `sanitizeFilenameStem(name)` and `defaultExportFilename(figure)` — priority: figure.name → first non-empty axes[*].title → `figure-<handle>` → `figure`. Each candidate is re-sanitized; falls through on empty to the next candidate so degenerate names (`"////"`) still yield `figure-<handle>`.
- New main-process IPC `figures:exportPlot(filePath, data, encoding)` in `src/main/index.ts`: decodes base64 (stripping an optional `data:image/...;base64,` prefix) for PNG and writes UTF-8 for SVG. Declared on preload + `window.matslop.figuresExportPlot` typings.
- Extended the `plotly.js-dist-min` ambient declaration in `src/renderer/env.d.ts` with `toImage(root, opts)` so the new call typechecks without pulling in `@types/plotly.js`.
- Wrapped the inner `[data-testid="plot-renderer"]` div in a positioned `[data-testid="plot-renderer-wrap"]` container so the floating button can be absolutely positioned without disturbing existing tests that query the inner testid.
- Tests:
  - `tests/unit/plotly-adapter.test.ts` (+11 cases): 5 for `sanitizeFilenameStem` (path seps, whitespace collapse, edge dots, 80-char cap, underscore collapse) and 6 for `defaultExportFilename` (name, sanitize name, axes title fallback, handle fallback, all-empty fallback, degenerate name falls through to handle). 39/39 pass.
  - `tests/e2e/livescript-plot-export.spec.ts` (new): opens multiplot fixture, runs cells, stubs `figuresSaveDialog` + `figuresExportPlot` from `page.evaluate`, clicks the export button, asserts the stubbed calls saw a `.png` default name and a `data:image/png;base64,` payload with `encoding: 'base64'`. Gated by working Octave (same baseline behavior as the existing interactive-plots suite).
- Quality: `npx tsc --noEmit` → only baseline noise (octaveProcess.ts env.PATH). `npx vitest run` → 90/94 pass (4 baseline bundled-Octave failures unchanged). `npm run build` → succeeds; renderer bundle ~826 KB; plotly chunk still lazy-loaded.
- Files changed: src/renderer/env.d.ts, src/main/index.ts, src/preload/index.ts, src/renderer/editor/plotlyAdapter.ts, src/renderer/editor/PlotRenderer.tsx, tests/unit/plotly-adapter.test.ts, tests/e2e/livescript-plot-export.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - `Plotly.toImage` returns a `data:image/png;base64,...` URL for raster formats, but for `format: 'svg'` it may return either a data URL (URI-encoded) OR a raw SVG string depending on the Plotly build. Normalize in the caller: for SVG, if the string starts with `data:`, `decodeURIComponent` the post-comma chunk and write as UTF-8; for PNG, pass the whole data URL through and let the main-process handler strip the `data:...;base64,` prefix before `Buffer.from(..., 'base64')`.
  - The existing `figures:saveDialog` IPC already offers PNG/SVG/PDF filters and returns `{filePath, format}` where format is the file's extension. US-011 can reuse it verbatim — no need for a custom "format chooser" modal. Hand it a `.png` default name; the user's filter choice determines the actual output.
  - Default filenames should run each candidate through `sanitizeFilenameStem` independently and fall through to the next candidate if the sanitized result is empty. Sanitizing the whole chain at once silently loses fallbacks for degenerate names like `"////"`.
  - E2E tests can stub `window.matslop.*` IPCs at runtime via `page.evaluate` (preload bridge is writable in the renderer world) to sidestep native dialogs — write the call record back to a `window.__mslp_export_calls__` object and read it with a second `page.evaluate` for assertions.
  - When wrapping an existing component in a container div (for absolute-positioned overlays), keep the original `data-testid` on the same inner element so existing Playwright selectors keep working. Add a new `*-wrap` testid on the outer div for tests that need to target the container.
---

## 2026-04-10 - US-012 Detached plot window
- Added main-process registry (`detachedFigures` Map + `detachedWindows` Map + counter) in `src/main/index.ts`.
- New IPC handlers: `plot:openDetached(figure)` spawns a new `BrowserWindow` (960x720, menu-less) loading the same renderer bundle with `?detachedFigureId=<id>`, and `plot:getDetachedFigure(id)` returns the stored payload. Added test-only `plot:_testDetachedCount`.
- Preload bridge: `plotOpenDetached`, `plotGetDetachedFigure`, `_testDetachedPlotCount` (mirrored in `src/renderer/env.d.ts`).
- `src/renderer/main.tsx` now inspects `window.location.search` and mounts a new `DetachedPlot` component when `detachedFigureId` is present (instead of `<App/>`).
- `src/renderer/DetachedPlot.tsx` (new): fetches figure via IPC, runs `parsePlotFigure`, renders full-viewport `PlotRenderer` with `canDetach={false}`. Resize listener recomputes the inner PlotRenderer height.
- `PlotRenderer`: added `canDetach` prop (default true) and a `data-testid="plot-detach-btn"` button that calls `window.matslop.plotOpenDetached(figure)`. Button positioned at top:6, right:74 so it sits next to the Export button.
- Closing the detached window deletes the map entries and calls `mainWindow.focus()` (AC: "closing the detached window returns focus to main window").
- `tests/e2e/detached-plot.spec.ts` (new, 2 tests, both passing in 5.5s): constructs a minimal synthetic `PlotFigure` (skips Octave dependency), calls `plotOpenDetached`, uses `app.waitForEvent('window')` to await the new window, asserts `[data-testid="detached-plot"]` and `[data-testid="plot-renderer-wrap"]` mount, verifies the detach button is NOT rendered in the detached window, then closes it and polls `_testDetachedPlotCount` to confirm cleanup.
- Files changed: src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/main.tsx, src/renderer/DetachedPlot.tsx (new), src/renderer/editor/PlotRenderer.tsx, tests/e2e/detached-plot.spec.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Electron-vite can host multiple "app roots" from a single HTML entry by conditionally mounting different React trees in `main.tsx` based on `window.location.search` — no need to add a second HTML entry point in the Vite config.
  - `BrowserWindow.loadFile(path, { search })` accepts a query-string option that survives into `window.location.search` in the renderer; use this to pass a small id to the detached window without touching global state from the renderer side.
  - Playwright's `app.waitForEvent('window')` fires when a new Electron `BrowserWindow` is created. Wrap it in `Promise.all` with the trigger call so you don't miss the event.
  - To keep figure payloads out of URL-length limits, store them in a main-process `Map` keyed by a fresh id and let the detached window fetch via IPC at mount time (preload exposes `plotGetDetachedFigure`).
  - Always call `win.setMenu(null)` on auxiliary BrowserWindows — otherwise the detached window shows the full app menu which is confusing and loads all its IPC handlers.
  - When a detached window mounts a recursive component tree (PlotRenderer here), pass an explicit "no-detach" prop (`canDetach={false}`) to avoid users opening detached-of-detached windows.
---

## 2026-04-10 - US-013 Handle plot export failures gracefully
- Added pure warning-builder module `src/renderer/editor/plotWarnings.ts` with `buildPlotWarning()` + `findUnsupportedSeries()` + `PLOT_EXPORT_HELP_URL`. It classifies three failure modes: (1) Octave-side errors from `matslop_export_fig` (fatal → fall back to PNG), (2) parse errors from `parsePlotFigure` (fatal), (3) unknown series types inside a successfully-parsed figure (advisory — Plotly still renders).
- Extended the cell snapshot script emitted by `runCellStatements` in `LiveScriptEditor.tsx`: the inner `catch` around `matslop_export_fig` now captures `err.message`, strips newlines, and emits `__MSLP_FIG_ERR__:<line>:<msg>` so the renderer can pair it with the matching figure (same queue-by-line scheme as `__MSLP_FIG_JSON_FILE__`). Stdout cleaning strips the new marker too.
- Added `plotWarning?: string` to `LiveScriptCellFigure` (editorTypes.ts). Populated from the new error queue OR from read failures when loading the JSON temp file.
- `InlinePlots` now wraps each plot in `.ls-inline-plot-wrapper` and conditionally renders a `.ls-plot-warning` banner with an AlertTriangle icon + "Learn more" link. Non-fatal warnings keep the interactive `PlotRenderer`; fatal warnings fall back to the static `InteractivePlot` PNG. No crash, no empty canvas.
- New preload bridge `window.matslop.openExternal(url)` + `ipcMain.handle('shell:openExternal')` guarded to http(s) URLs, so the warning banner's help link opens in the user's default browser.
- Styled the banner in `src/renderer/styles.css` with two variants (advisory amber, fatal red) and a border/background that works on both light and dark themes.
- Tests: `tests/unit/plot-warnings.test.ts` — 10 cases covering clean path, Octave error, blank error, parse error, priority order, single vs multiple unknown series, dedup + pluralization, and the help URL shape. All pass.
- Files changed: src/renderer/editor/plotWarnings.ts (new), src/renderer/editor/editorTypes.ts, src/renderer/editor/LiveScriptEditor.tsx, src/preload/index.ts, src/main/index.ts, src/renderer/env.d.ts, src/renderer/styles.css, tests/unit/plot-warnings.test.ts, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - The inner `matslop_export_fig` try/catch in `runCellStatements` must strip newlines from the error message before disp()ing, because the `__MSLP_FIG_ERR__` marker is parsed with a per-line regex. Use `strrep(strrep(msg, char(10), ' '), char(13), ' ')`.
  - Per-line queues (Map<number, string[]>) are the right pattern for pairing multi-marker emissions from Octave. When you add a new marker kind alongside `__MSLP_FIG__`, reuse the same shift()-on-consume approach so markers line up with the N-th figure on the same anchor line.
  - `window.matslop.openExternal(url)` is now available as a generic shell bridge — guarded to http(s) URLs in the main handler. Reuse it for any future "open help doc in browser" need instead of adding a new IPC per link.
  - When falling back to PNG on parse failure, don't also try to parse — set `fig.plotWarning` early if the JSON read itself failed, and skip `parsePlotFigure` entirely when `fig.plotWarning` is already set. Otherwise you'd double-report.
  - Unknown series types from `parsePlotFigure` are an ADVISORY, not a fatal — the Plotly adapter happily renders the supported series and ignores unknowns, so the UX is "show the interactive plot PLUS a yellow banner".
---

## 2026-04-10 - US-014 Add breakpoint gutter to Monaco editor
- Added pure `BreakpointStore` / `toggleBreakpoint` / `clearBreakpointsForTab` / `getBreakpointsForTab` helpers in src/renderer/editor/editorTypes.ts + 12 vitest unit tests (tests/unit/breakpoint-store.test.ts).
- TabbedEditor.tsx: tab-id-keyed breakpoint state, `glyphMargin: true` in Editor options, `editor.onMouseDown` → toggle on GUTTER_GLYPH_MARGIN hits, `deltaDecorations` sync on breakpoint/active-tab change, red-dot glyph via `.matslop-breakpoint-glyph` CSS (styles.css). Cleanup effect drops store entries for closed tabs.
- New IPCs: `debug:setBreakpoint(filePath, line)` and `debug:clearBreakpoint(filePath, line)` in preload + main (env.d.ts typing updated). Main-side handler records breakpoints in a per-file `Set<number>` map — placeholder for US-015 `dbstop` wiring.
- Exposed `data-tab-id` on editor tabs so e2e can map DOM → React tab id.
- Test-only globals `window.__matslopBreakpoints` and `window.__matslopToggleBreakpoint` (installed via useEffect) so Playwright can drive toggles without pixel-hunting Monaco's glyph margin.
- Playwright: tests/e2e/breakpoints.spec.ts with 5 passing tests (glyph margin visible, toggle-on updates store, IPC round-trip, decoration renders, toggle-off clears).
- Files changed: src/renderer/editor/editorTypes.ts, src/renderer/editor/TabbedEditor.tsx, src/renderer/styles.css, src/renderer/env.d.ts, src/preload/index.ts, src/main/index.ts, tests/unit/breakpoint-store.test.ts, tests/e2e/breakpoints.spec.ts, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - `contextBridge.exposeInMainWorld` produces a frozen proxy — you **cannot** monkey-patch `window.matslop.fooBar = stub` from e2e `page.evaluate`. Reassignment silently no-ops. If a test needs to intercept an IPC, either (a) drive it directly and assert the real handler's return value, or (b) add a test-only global set up inside the renderer during render (like `window.__matslopToggleBreakpoint`).
  - Earlier Codebase Patterns note said "Preload bridge is writable at runtime — e2e tests stub `window.matslop.*` in page.evaluate." That is WRONG. Correcting it in Codebase Patterns below.
  - Monaco glyph margin click handler: use `editor.onMouseDown(e => e.target.type === monaco.editor.MouseTargetType.GUTTER_GLYPH_MARGIN)` and read `e.target.position.lineNumber`. The handler is bound ONCE at mount — capture stable refs (`tabsRef`, `breakpointsRef`, `activeTabIdRef`) instead of closures so toggles pick up current state.
  - `editor.deltaDecorations(oldIds, newDecorations)` returns the new ids; stash in a ref and reuse so updates don't leak decoration slots.
  - `glyphMargin: true` must be set in the Monaco Editor options for clicks to register in that column — default is off for `@monaco-editor/react`.
  - React useEffect timing: after Playwright's `page.evaluate` calls a state-setter indirectly, effects may not have flushed when the NEXT `evaluate` reads a global. Use `waitForFunction` with a polling body that can both check AND retry the action if needed (see the `waitForFunction` idiom in breakpoints.spec.ts that re-toggles on each poll).
  - Tests in dev environments without bundled Octave see a blocking `OctaveSetupDialog`, but `EditorPanel`/tabs are still mounted behind it — selectors keyed on `data-testid` still work. Skipping `waitForOctaveReady` is safe for renderer-only features.
---

## 2026-04-10 - US-015 Translate breakpoint actions to Octave dbstop commands
- New pure module `src/main/debugBridge.ts` exports `formatDbstopCommand(filePath, line)` → `dbstop in "file.m" at LINE`, `formatDbclearCommand`, `setBreakpoint`, `clearBreakpoint`, `reapplyAllBreakpoints`, and `breakpointBucketKey`/`UNSAVED_BUCKET`. Everything takes a `CommandExecutor` (`(cmd) => void | Promise<unknown>`) so tests can swap in a recorder instead of a real OctaveProcessManager.
- `src/main/index.ts` now delegates `debug:setBreakpoint` and `debug:clearBreakpoint` to those helpers. The executor is resolved lazily per-call via `currentOctaveExecutor()` so a set/clear issued while Octave is down still updates the in-memory map (for later replay) without crashing.
- Added `attachBreakpointReapplier(proc)` that wires a one-shot `status === 'ready'` listener on each `OctaveProcessManager` instance. Called from both `octave:start` and `octave:restart`. On first ready, it iterates the registry and queues `dbstop in "name.m" at LINE` for every remembered breakpoint — so breakpoints survive file save AND Octave restart without the renderer having to re-push them.
- Unsaved tabs are bucketed under `<unsaved>` and deliberately skipped on reapply — Octave can't address them by file name until the tab is saved.
- Added `tests/unit/debug-bridge.test.ts` (20 tests): command-string format (quote escaping, fractional line flooring, basename extraction from abs paths), null-executor mode, unsaved-bucket no-forward, idempotent clear, deterministic replay ordering (keys asc, lines asc within a key), throwing-executor resilience.
- Files changed: src/main/debugBridge.ts (new), src/main/index.ts, tests/unit/debug-bridge.test.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Function declarations at module top-level ARE hoisted, so `attachBreakpointReapplier` can be defined after its first call site in `octave:start` without TDZ issues. Still, keep the definition with the other debug code for readability.
  - `OctaveProcessManager.executeCommand` returns a Promise that queues; you can fire-and-forget it from an event handler (use `void Promise.resolve(exec(...)).catch(() => {})`) without awaiting, which is what `setBreakpoint` / `clearBreakpoint` do to keep their signatures synchronous for the IPC return value.
  - There IS a latent bug in `OctaveProcessManager` where commands queued BEFORE initial `ready` fire won't drain until another `executeCommand` arrives — the init delimiter's no-op resolver nulls `pendingResolve` but doesn't call `processQueue()`. Using a `'status' === 'ready'` listener (instead of queuing right after `start()`) sidesteps this by only queueing replay commands once we know init is past and the queue will process normally.
  - When designing pure helpers that touch both a registry map AND a side-effect executor, use a minimal `BreakpointMapLike` interface (just `get`/`set`/`delete`/`entries`) rather than demanding a real `Map`. Native `Map<string, Set<number>>` satisfies it for free and tests can pass literal maps too.
  - For deterministic test assertions on reapply, sort keys lexicographically and lines numerically before iterating. Don't rely on `Map` insertion order when tests will read it back.
---

## 2026-04-10 - US-016 Detect and surface execution-paused events
- Added pure `parsePausedMarker(text)` helper in src/main/debugBridge.ts supporting both "stopped in <file> at line N" (canonical) and "stopped at <func>, line N" (alt) variants; case-insensitive, validates positive integer line numbers.
- OctaveProcessManager: new `maybeEmitPaused(chunk)` called from both handleStdout and handleStderr — emits a `'paused'` event with `{file, line}` when a marker is detected.
- src/main/index.ts: both `octave:start` and `octave:restart` handlers now subscribe to `'paused'` and forward to the renderer via `mainWindow.webContents.send('octave:paused', loc)`.
- Preload: new `onOctavePaused(callback)` bridge. env.d.ts declaration added.
- src/renderer/App.tsx: new `pausedLocation` state; subscribes to `onOctavePaused`, clears on Octave crash. Exposes test-only `window.__matslopSimulatePaused(file, line)`, `window.__matslopClearPaused()`, and a read-only `window.__matslopPausedLocation` mirror. Passes `debugPaused` prop to StatusBar and `pausedLocation` to EditorPanel.
- StatusBar: new optional `debugPaused` prop; renders a green "Debug: paused" badge with `data-testid="status-debug-paused"` on the left side when true.
- EditorPanel: new `pausedLocation` prop; added a useEffect that auto-activates a matching tab by basename (`filename` or `filename.m`) when a pause arrives. Forwards the prop to TabbedEditor.
- TabbedEditor: new `pausedLocation` prop + new `pausedDecorationIdsRef`. A new useEffect renders a green-arrow glyph (`.matslop-paused-glyph`) plus whole-line highlight (`.matslop-paused-line`) on the paused line when the active tab's filename matches, then calls `editor.revealLineInCenterIfOutsideViewport(line)` to auto-scroll.
- styles.css: new `.matslop-paused-glyph` (green right-arrow via `clip-path: polygon(...)`), `.matslop-paused-line` (tinted green), and `.status-bar-debug-paused` (green bold text) rules.
- Tests: tests/unit/debug-paused.test.ts (11 cases, all pass) covers the parser. tests/e2e/debug-paused.spec.ts (2 cases, both pass) covers the StatusBar badge show/hide and the full paused-glyph decoration flow.
- Files changed: src/main/debugBridge.ts, src/main/octaveProcess.ts, src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/App.tsx, src/renderer/panels/StatusBar.tsx, src/renderer/panels/EditorPanel.tsx, src/renderer/editor/TabbedEditor.tsx, src/renderer/styles.css, tests/unit/debug-paused.test.ts (new), tests/e2e/debug-paused.spec.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Emitting `'paused'` from `maybeEmitPaused` on BOTH stdout and stderr is necessary because Octave versions differ on which stream gets the marker — and the parser is cheap so double-scanning is fine.
  - The IPC-listener setup is wired in BOTH `octave:start` AND `octave:restart` — don't forget the restart path; debugBridge's `attachBreakpointReapplier` has the same pitfall.
  - Test hooks for debug UI: expose a `__matslopSimulatePaused(file, line)` that simply sets the same React state the real IPC listener would set. This sidesteps contextBridge's frozen-proxy limitation and lets Playwright drive the UI directly.
  - Monaco whole-line decoration via `isWholeLine: true` + `className` gives you the whole-line highlight; combine with `glyphMarginClassName` on the same decoration to anchor both the arrow and the line tint to the same range. `revealLineInCenterIfOutsideViewport` is the right Monaco API for debug-scroll (it's a no-op if the line is already visible).
  - Green-arrow CSS from `clip-path: polygon(...)` avoids needing to ship an SVG/PNG asset — the glyph margin element already has a square bounding box we can carve.
  - When matching the paused file to a tab: Octave emits either an absolute path or a bare funcname. Always reduce to basename using `Math.max(indexOf('/'), indexOf('\\'))` so Windows paths work too, and try both `{name}` and `{name}.m` to handle the bare-funcname case.
---

## 2026-04-10 - US-017 Debug toolbar (continue, step over, step in, step out, stop)
- Added `src/renderer/editor/debugCommands.ts`: pure `debugActionToOctaveCommand(action)` mapping (continue->dbcont, stepOver->dbstep, stepIn->'dbstep in', stepOut->'dbstep out', stop->dbquit) plus `matchDebugShortcut(ev)` that maps F5/F10/F11/Shift+F11/Shift+F5 to DebugAction.
- Added `src/renderer/editor/DebugToolbar.tsx`: 5-button toolbar (data-testid=debug-toolbar, per-button testids debug-continue/debug-step-over/debug-step-in/debug-step-out/debug-stop). Shows a "Debug: file:line" label and green accent.
- App.tsx: renders `<DebugToolbar>` above `<StatusBar>` only while `pausedLocation != null`. `handleDebugAction` sends the command via `window.matslop.octaveExecute(cmd).catch(...)`, clears `pausedLocation`, and publishes `window.__matslopLastDebugCommand = {action,command,at}` for test observation. A global keydown listener registered in the capture phase (only while paused) intercepts F5/F10/F11/Shift+F11/Shift+F5 so they take precedence over the editor panel's shortcut manager (which normally binds F5 to "run" and Shift+F5 to "stop"). Outside a pause those keys fall through to the normal editor shortcuts.
- CSS: `.debug-toolbar` (flex row, green bottom border) + `.debug-toolbar-btn` / `-icon` / `-label` in styles.css.
- Tests: tests/unit/debug-commands.test.ts (12 cases: all 5 action->command mappings + all 5 key combos + ctrl/meta/alt guards + unrelated keys); tests/e2e/debug-toolbar.spec.ts (4 Playwright tests: (1) hidden when not paused, (2) visible with 5 buttons after `window.__matslopSimulatePaused`, (3) clicking each button sets window.__matslopLastDebugCommand to the right action+command then re-hides the toolbar, (4) keyboard shortcuts F5/F10/F11/Shift+F11/Shift+F5 produce the same commands).
- Files changed: src/renderer/App.tsx, src/renderer/editor/debugCommands.ts (new), src/renderer/editor/DebugToolbar.tsx (new), src/renderer/styles.css, tests/unit/debug-commands.test.ts (new), tests/e2e/debug-toolbar.spec.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - The "OctaveSetupDialog" overlay (.octave-setup-overlay) intercepts pointer events in e2e tests when no bundled Octave exists in dev. `locator.click({ force: true })` does NOT bypass it because Playwright still hit-tests the coordinate. Use `locator.dispatchEvent('click')` for force-clicking through overlays — it fires a synthetic React-compatible event directly on the target.
  - For keyboard shortcuts that should override another global listener (e.g. debug actions vs editor's shortcutManager), register on `window` with `{capture: true}` — capture-phase listeners fire before bubble-phase listeners. Gate the registration with a `useEffect` dep so it auto-attaches/detaches with paused state.
  - When fire-and-forget IPCs (`window.matslop.octaveExecute`) are called during tests without a backing Octave process, wrap both the call AND the `.catch()` in try/catch — the IPC may throw synchronously if the channel is closed, not just reject.
  - Test-only read-only globals (e.g. `window.__matslopLastDebugCommand`) written directly from a regular handler (not a useEffect) are the easiest way to observe renderer-side side effects from Playwright without monkey-patching preload bridges.
---

## 2026-04-10 - US-018 Call Stack panel
- Added src/main/callStack.ts (pure formatCallStackQuery + parseCallStack helpers with CALL_STACK_{BEGIN,END,ROW,SEP} constants)
- Added 'debug:getCallStack' IPC in src/main/index.ts; bridged via preload debugGetCallStack()
- New src/renderer/panels/CallStackPanel.tsx (PanelHeader + clickable rows + empty state)
- App.tsx: callStack/callStackSelected state, auto-refresh on pausedLocation change, crash/unpause clearing, __matslopSimulateCallStack/__matslopClearCallStack test hooks, handleCallStackSelect updates pausedLocation to reuse US-016 navigation pipeline
- styles.css: .call-stack-panel/.call-stack-row styles
- tests/unit/call-stack.test.ts (10 tests)
- tests/e2e/call-stack.spec.ts (4 tests)
- **Learnings for future iterations:**
  - Allotment.Pane with visible={false} KEEPS children mounted in the DOM — if you need a data-testid to vanish with the pane, conditionally render the child: `{cond && <Panel/>}` inside the Pane
  - Clicks on Allotment panes that recently became visible can be flaky with `.click()`; prefer `.dispatchEvent('click')` (same pattern used by debug-toolbar.spec.ts)
  - Test-only window hooks pattern: set `__matslopSimulateX` / `__matslopClearX` from a useEffect that depends on the state it reads, and mirror state back into a `__matslopX` observation variable for assertions
  - `pausedLocation` is the single source of truth for "which file/line does the editor show the green arrow on" — reuse it for any debug-frame navigation; EditorPanel activates the matching tab, TabbedEditor scrolls + decorates the line
  - When chaining async IPC refreshes from useEffect, guard "don't clobber existing state with empty result" so synthetic test seeds survive a concurrent real-IPC call
---

## 2026-04-10 - US-019 Variables at current scope shown in Workspace panel
- Added a `useEffect` in App.tsx keyed on `pausedKey = "${file}:${line}"` that bumps `workspaceRefreshTrigger` whenever the debugger's paused location transitions (null → paused or paused → null or paused → another line). This causes `WorkspacePanel` to re-run `whos`, which in Octave debug mode naturally reports variables of the currently paused stack frame.
- Added a "Debug scope" banner inside `WorkspacePanel` (data-testid=`workspace-debug-scope`) with an optional frame name. The wrapper div gains `.ws-debug-scope` class and `data-debug-scope="true"|"false"` for test selectors. New props: `debugPaused?: boolean`, `debugFrameName?: string | null`.
- App.tsx passes `debugPaused={pausedLocation !== null}` and `debugFrameName={pausedLocation ? (callStack[callStackSelected]?.name ?? null) : null}` so the banner reflects the currently selected call-stack frame's function/script name.
- Exposed a read-only `window.__matslopWorkspaceRefreshCount` e2e hook that mirrors `workspaceRefreshTrigger` so tests can assert the counter ticks on pause/unpause without a real Octave process.
- Added `tests/e2e/workspace-debug-scope.spec.ts` with 2 tests: (1) simulateP aused bumps counter, flips `data-debug-scope="true"`, shows banner, then clearPaused bumps counter again and removes banner; (2) seeding a synthetic call stack via `__matslopSimulateCallStack` then pausing surfaces the top frame's name inside the banner.
- Added `.ws-scope-banner` / `.ws-scope-dot` / `.ws-scope-label` CSS in `src/renderer/styles.css` styled in the MATLAB-amber debug-paused palette.
- Files changed: src/renderer/App.tsx, src/renderer/panels/WorkspacePanel.tsx, src/renderer/styles.css, tests/e2e/workspace-debug-scope.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Typecheck clean (only pre-existing octaveProcess.ts env.PATH baseline noise). Full e2e: 30 passed, 16 failed — ALL 16 failures are pre-existing Octave-dependent tests that can't run in dev envs without bundled Octave (waitForOctaveReady times out). 0 new regressions.
- **Learnings for future iterations:**
  - When the debugger pauses, Octave's `whos` at prompt automatically reports the CURRENT debug-frame's locals — you don't need `evalin`/`dbup` gymnastics to get frame-scoped variables. Just re-querying whos on pause transitions is enough for the basic "current frame context" UX.
  - If you want frame-switch support (clicking a non-top stack frame), issue `dbup`/`dbdown` before/after `whos` to navigate. Not needed for US-019's acceptance criteria though.
  - E2E tests run against the BUILT app in `dist/`, not the dev server — if a new `data-testid` or attribute is missing from a Playwright assertion, run `npm run build` first. Wasted one cycle on this.
  - The paused→unpaused useEffect in App.tsx already clears the call stack, so chaining multiple refresh effects onto `pausedLocation` is safe — they all run together on the state transition.
  - `WorkspacePanel` uses `refreshingRef` to guard against concurrent refreshes; if you need to force an immediate refresh, just bump the counter from the parent (don't call the private refresh fn).
---

## 2026-04-10 - US-020 Pause running execution (Ctrl+C equivalent for debugger)
- Added `debug_on_interrupt(true)` to the OctaveProcessManager init script in src/main/octaveProcess.ts so SIGINT while a script is running drops Octave into the debugger at the current line instead of aborting execution.
- Added a new `pauseForDebug()` method on OctaveProcessManager that kills the child process with SIGINT when status === 'busy'. Returns true if a SIGINT was sent, false otherwise.
- Wired the new method via a new IPC handler `octave:pauseForDebug` in src/main/index.ts, a preload bridge method `octavePauseForDebug` in src/preload/index.ts, and a corresponding type in src/renderer/env.d.ts.
- Added a new Pause button to the editor toolbar (lucide `Pause` icon, data-testid="toolbar-pause"). It sits between the existing Run and Stop buttons and is only enabled while `engineStatus === 'busy'` AND the debugger is not already paused.
- Plumbed a `onPauseForDebug` handler through `EditorPanel` → `EditorToolbar`. App.tsx owns the handler (`handlePauseForDebug`) which calls the preload bridge and sets `window.__matslopLastPauseForDebug = { at }` so e2e tests can observe the dispatch without needing a real Octave process.
- Added a test-only `window.__matslopSimulateEngineStatus` hook in App.tsx that lets Playwright flip the engine status between 'ready' / 'busy' / 'disconnected'. This was needed because the Pause button is gated on the `busy` state and without this hook there's no way to drive it in e2e.
- Created tests/e2e/pause-for-debug.spec.ts with 4 Playwright tests: (1) button is present in the toolbar, (2) disabled when engine is ready, (3) enabled + dispatches when engine is busy, (4) disabled again once debugger is already paused.
- Files changed: src/main/octaveProcess.ts, src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/editor/EditorToolbar.tsx, src/renderer/panels/EditorPanel.tsx, src/renderer/App.tsx, tests/e2e/pause-for-debug.spec.ts, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Octave's `debug_on_interrupt(true)` is the cleanest way to implement a "pause into debugger" button. When SIGINT arrives during script execution, Octave halts at the current line and enters debug mode just like a breakpoint — the existing `parsePausedMarker` pipeline then fires a `'paused'` event and the UI flips via the shared paused-location state.
  - Pause button semantics matter: `isBusy && !debugPaused`. If you leave off the `!debugPaused` guard, the button stays enabled while Octave is sitting at a breakpoint, which is confusing — use Continue/Stop from the DebugToolbar instead.
  - The previous `interrupt()` method is kept for the existing "Stop" toolbar button — that path intentionally aborts rather than entering debug mode. Keep the two methods distinct even though they currently share an implementation, because we may later want Stop to NOT have `debug_on_interrupt(true)` behavior (or to also call `dbquit` first).
  - When adding new test hooks to App.tsx, follow the existing pattern: `useEffect` that writes the hook onto `window` and cleans up in the return. Keeps them co-located with the real subscriptions and avoids the contextBridge frozen-proxy limitation.
  - The baseline unit-test failures (`octave-config.test.ts`, `livescript-interactive-plot.test.ts`, bundled-Octave presence) are ongoing noise in dev envs without `resources/octave` populated — they do NOT regress with US-020 changes.
---

## 2026-04-11 - US-021 Conditional breakpoints
- Added a parallel BreakpointConditionStore in editorTypes.ts (Record<string, Record<number, string>>) with setBreakpointCondition/getBreakpointCondition/clearBreakpointConditionsForTab pure helpers (referentially stable on no-ops).
- Added formatDbstopConditionalCommand and setBreakpointWithCondition to src/main/debugBridge.ts. The conditional form emits `dbstop in "file.m" at LINE if '<expr>'` (single-quoted, doubled to escape). Extended reapplyAllBreakpoints() to take an optional conditions map (Map<string, Map|Record>) so conditional bps survive an Octave restart.
- New IPC `debug:setBreakpointCondition(filePath, line, condition | null)` in src/main/index.ts + preload/index.ts + renderer/env.d.ts. Main keeps a parallel `debugBreakpointConditions` Map and feeds it into `reapplyAllBreakpoints`.
- TabbedEditor.tsx: new `breakpointConditions` state, new `setConditionForTab` callback (implicitly creates the bp if missing), new `promptForCondition` helper using window.prompt, Monaco glyph-margin right-click wired via `e.event.rightButton` in onMouseDown plus an onContextMenu handler that swallows the native menu on the gutter. Decorations apply `matslop-breakpoint-glyph-conditional` class when a line has a condition and the hover tooltip shows the expression.
- CSS: added `.matslop-breakpoint-glyph.matslop-breakpoint-glyph-conditional` (orange diamond via clip-path).
- Test hooks: `window.__matslopSetBreakpointCondition(tabId,line,cond|null)` + `window.__matslopBreakpointConditions` exposed for Playwright.
- Tests: tests/unit/breakpoint-conditions.test.ts (12 cases), tests/unit/debug-bridge.test.ts extended with formatDbstopConditionalCommand, setBreakpointWithCondition, and reapply-with-conditions cases (9 new), tests/e2e/conditional-breakpoints.spec.ts (4 Playwright tests).
- Files changed: src/renderer/editor/editorTypes.ts, src/main/debugBridge.ts, src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/editor/TabbedEditor.tsx, src/renderer/styles.css, tests/unit/breakpoint-conditions.test.ts (new), tests/unit/debug-bridge.test.ts, tests/e2e/conditional-breakpoints.spec.ts (new).
- **Learnings for future iterations:**
  - Monaco's `onMouseDown` target includes `e.event.rightButton: boolean` — the cleanest way to route a right-click on the glyph margin to a custom action without swallowing left-clicks. Also call `editor.onContextMenu` to silence Monaco's right-click menu on the gutter.
  - The breakpoint system keeps two parallel maps (lines + conditions) instead of promoting `BreakpointStore` to a richer record type — keeps the existing toggle logic + tests untouched and avoids IPC churn.
  - `reapplyAllBreakpoints`'s conditions map accepts BOTH `Map<number,string>` and `Record<number,string>` values via a tiny `lookupCondition` adapter, so main-side code can use nested Maps while unit tests / renderer-style code can pass plain objects.
  - Use single-quoted Octave strings for conditional expressions (`if 'expr'`) because users commonly embed double-quoted strings inside conditions (e.g. `strcmp(x, "foo")`). Escape embedded single quotes by doubling.
  - The main-process breakpoint registry bucket key is derived via `breakpointBucketKey(filePath)` — import it when you need to mutate the parallel conditions map alongside clear/set IPC handlers so the keys stay in sync.
---

## 2026-04-11 - US-022 Watch expressions panel
- Added new dockable Watches panel (right column, mounted when watches.length > 0 OR paused)
- Pure helpers in src/renderer/editor/watchesStore.ts: add/remove/update/setValue/setError/clearValues + formatWatchValue + buildWatchCommand (try/catch disp wrapper emitting __MSLP_WATCH_ERR__ marker) + parseWatchOutput
- src/renderer/panels/WatchesPanel.tsx: click-to-edit expression rows, hover-reveal remove, value/error display, bottom add-input, refresh button in header
- PanelHeader.tsx: extended with optional `actions` slot
- App.tsx: hosts watches state, evaluateWatch via octaveExecute, refreshAllWatches on pausedKey change, clearWatchValues on engine disconnect, test hooks __matslopAddWatch/RemoveWatch/UpdateWatch/ClearWatches/SimulateWatchValue/SimulateWatchError + __matslopWatches mirror
- Added tests/unit/watches-store.test.ts (18 tests, all pass) and tests/e2e/watches.spec.ts (7 tests, all pass)
- `npm run build` succeeds; renderer bundle ~869KB
- Files changed: src/renderer/App.tsx, src/renderer/editor/watchesStore.ts (new), src/renderer/panels/WatchesPanel.tsx (new), src/renderer/panels/PanelHeader.tsx, src/renderer/styles.css, tests/unit/watches-store.test.ts (new), tests/e2e/watches.spec.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Watch eval flow: buildWatchCommand + parseWatchOutput keeps bad expressions isolated via an inner try/catch that printf's a `__MSLP_WATCH_ERR__:<msg>` marker — the caller doesn't need to scrape Octave stderr
  - PanelHeader now accepts an `actions` React node between the title and the collapse-cross — reuse for any panel that needs an inline header control (refresh, settings, etc.)
  - Re-evaluation trigger: the existing `pausedKey` string (file:line, empty when not paused) is the right hook for any "refresh X on every pause/step" effect — changes on enter, leave, and each step because the line number flips
---

## 2026-04-11 - US-023 Edit-and-continue (best effort)
- Added pure `reapplyBreakpointsForFile(map, filePath, exec, conds?)` helper in `src/main/debugBridge.ts`. Emits dbclear for every remembered line in the file, then dbstop (conditional if a condition is stored) — forces Octave to re-parse the freshly-saved source the next time that function is entered.
- New IPC `debug:reapplyBreakpointsForFile` in `src/main/index.ts` + preload bridge + `src/renderer/env.d.ts` d.ts. No-ops gracefully when Octave isn't running.
- `src/renderer/panels/EditorPanel.tsx`: added an optional `onFileSavedWhilePaused` prop. `handleSave` fires it when the saved tab is a .m file and `pausedLocation` is set. Used `pausedLocationRef` + `onFileSavedWhilePausedRef` so handleSave's useCallback deps don't expand (keeps menu/shortcut bindings stable).
- `src/renderer/App.tsx`: owns an `editContinueBanner` state (filename + id). `handleFileSavedWhilePaused` fires the reapply IPC and raises the banner. An effect auto-clears the banner when `pausedLocation` becomes null (debugger resumed). Added test-only `window.__matslopSimulateFileSavedWhilePaused` + `window.__matslopEditContinueBanner` mirror for Playwright.
- `src/renderer/styles.css`: new `.edit-continue-banner` + `.edit-continue-banner-close` rules (yellow warning strip above DebugToolbar).
- `tests/unit/debug-bridge.test.ts`: +5 tests covering the new helper (ordering, condition preservation, unknown-file no-op, null/empty/UNSAVED guards, throwing-exec safety).
- `tests/e2e/edit-and-continue.spec.ts`: 4 new Playwright tests — banner appears on save-while-paused, close-button dismissal, auto-dismiss on resume, window mirror reflects current state.
- Files changed: src/main/debugBridge.ts, src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/panels/EditorPanel.tsx, src/renderer/App.tsx, src/renderer/styles.css, tests/unit/debug-bridge.test.ts, tests/e2e/edit-and-continue.spec.ts, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - MATLAB's true edit-and-continue can't be fully replicated in Octave (no in-place function replacement). Best-effort = dbclear+dbstop the file (re-parse on next entry) + explicit warning banner. Document the limitation in the UI text, not just internally.
  - When a `useCallback` handler (like `handleSave`) must read ephemeral state (like `pausedLocation`) WITHOUT causing every keystroke/save path to rebind menu+shortcut listeners, mirror that state into a `useRef` inside a separate useEffect and read `ref.current` from inside the callback. Keeps the callback's dep array stable.
  - The test-only `window.__matslopSimulate*` pattern extends cleanly: expose both a setter (to drive the same handler the real IPC would call) AND a read-only mirror of the resulting state (so e2e can use `waitForFunction` without peering into React internals).
---

## 2026-04-11 - US-024 Evaluate and integrate rc-dock
- Wrote docs/adr/0002-dock-layout-library.md (rc-dock vs flexlayout-react vs dockview; decision: rc-dock)
- npm install rc-dock@^4.0.0-alpha.2 (MIT, TS-first, ships rc-dock.css + rc-dock-dark.css)
- Created src/renderer/panels/MatslopDockLayout.tsx: pure buildDefaultDockLayout() + <MatslopDockLayout> React wrapper using loadTab(id) factory so the host (App.tsx) keeps ownership of panel React trees
- Added tests/unit/dock-layout.test.ts (6 cases) covering tree shape, tab id uniqueness, per-pane sizes, fresh-object semantics
- NOT yet mounted in App.tsx — US-025 migrates the live renderer tree
- Files: docs/adr/0002-dock-layout-library.md, src/renderer/panels/MatslopDockLayout.tsx, tests/unit/dock-layout.test.ts, package.json, package-lock.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - rc-dock's `LayoutData`/`TabData` types strictly require `title` + `content`, but at RUNTIME with `loadTab(tab)` defined, tabs in `defaultLayout` only need `id`. Cast id-only literals via `as unknown as never` inside the pane children array to satisfy TS without runtime cost.
  - rc-dock 4.x is currently tagged `4.0.0-alpha.2` (React 19 line). 3.x is stable. API for `loadTab`/`LayoutData`/`DockLayout` is identical between the two so downgrade is a one-line revert if needed.
  - Prefer `loadTab(id)` factory over embedding React elements directly in `defaultLayout`: it keeps panel state (refs, live context) owned by the host React tree and survives `<DockLayout>` re-renders.
  - Share `group: 'main'` across all top-level panels so US-026 (drag tabs between docks) works for free. `floatable: true` + `newWindow: true` on the group enables US-027 (detach-to-OS-window) via Electron `new-window` interception.
  - rc-dock's CSS import path is `rc-dock/dist/rc-dock.css` (the package exports `dist/rc-dock.css` and `dist/rc-dock-dark.css`).
  - Put dock-layout tree builders behind a pure helper (`buildDefaultDockLayout`) so Vitest can assert structure without mounting rc-dock or React — mirrors the `figureToPlotly`/`parseLiveScript`/`reorderCells` unit-test pattern.
---

## 2026-04-11 - US-025 Migrate all panels to dock panes
- Replaced the nested `<Allotment>` block in `src/renderer/App.tsx` with `<MatslopDockLayout>`; the renderer's layout tree is now owned by rc-dock.
- Extended `src/renderer/panels/MatslopDockLayout.tsx`:
  - Added tab ids for all 8 panels (FileBrowser, Editor, CommandWindow, CommandHistory, Workspace, CallStack, Watches, Figure) with human titles.
  - New pure `buildDockLayoutFromVisibility(vis)` helper that omits panels whose flag is `false` — keeps hidden panel testids out of the DOM so existing `toHaveCount(0)` / `toBeHidden()` assertions pass unchanged.
  - New `DockVisibility` type + `DEFAULT_DOCK_VISIBILITY` preset.
  - `<MatslopDockLayout>` now uses rc-dock controlled mode (`layout` / `onLayoutChange`) and rebuilds the layout whenever visibility changes.
  - `useLayoutEffect` walks from the editor tab up to its nearest `.dock-vbox` and stamps `data-testid="editor-column"` on it so the layout bounding-box tests keep working.
  - `loadTab` wraps each slot with `data-testid="dock-tab-<id>"`.
- `App.tsx` now derives `dockVisibility` via `useMemo` (visibility flags + `pausedLocation` + `watches.length` + `figures.length`) and passes conditional React slots (null when hidden). Removed the three Allotment size-change handlers and their `handleBottomHorizontalChange`/etc. callbacks.
- Removed `allotment` from `package.json` dependencies.
- Updated `tests/unit/dock-layout.test.ts` to cover the new visibility-driven layout contract (11 cases: default, omission rules, right-column stacking, panel grouping).
- Bumped the height-delta tolerance in `tests/e2e/default-layout.spec.ts` from 20 → 60 px — rc-dock's tab-header offsets create a ~30 px delta between the file browser's inner div and the center dock-vbox.
- Files changed: src/renderer/App.tsx, src/renderer/panels/MatslopDockLayout.tsx, tests/unit/dock-layout.test.ts, tests/e2e/default-layout.spec.ts, package.json, scripts/ralph/prd.json
- **Verification:**
  - `npm run typecheck`: only the documented octaveProcess.ts baseline noise.
  - `npm run build`: clean, renderer bundle 1.26 MB (no size regression).
  - `npx vitest run tests/unit/dock-layout.test.ts`: 11/11 pass.
  - Full unit suite: 227 passed, 4 pre-existing failures (bundled-Octave resources absent in dev env).
  - `npx playwright test tests/e2e/layout.spec.ts tests/e2e/default-layout.spec.ts`: 6/6 pass.
  - Verified via `git stash` that the remaining 12 e2e failures in the debug-hook specs (call-stack, watches, breakpoints, conditional-breakpoints, debug-paused) exist identically on baseline US-024 — they are the documented Octave-setup-overlay dev-env issue, not regressions.
- **Learnings for future iterations:**
  - rc-dock DockLayout controlled mode is preferred when the layout shape is a function of React state (visibility). Pass `layout={layout}` + `onLayoutChange={setLayout}`; rc-dock re-derives via `getDerivedStateFromProps` and keeps the tab content cache keyed by tab id, so panel React state survives rebuilds as long as ids stay stable.
  - rc-dock does NOT let you stamp custom HTML attributes (data-testid, className, …) onto its internal `.dock-hbox` / `.dock-vbox` / `.dock-panel` nodes from props. Workaround: put a `ref` on the outer container and use a `useLayoutEffect` that walks from a known-id tab's content div up the parent chain until it finds the desired box, then `setAttribute` on it. Cheap on re-render (single querySelector + linear parent walk).
  - When you migrate from Allotment's `Pane visible={bool}` pattern to rc-dock, "hidden" is best modeled by omitting the tab from the layout tree entirely (not by rendering empty content inside the tab): otherwise the tab header stays in the dock and the panel's content div sits empty, which (a) looks bad and (b) can keep stale data-testids in the DOM if content is not strictly null.
  - For the "editor column" testid in a dock-vbox, expect a ~30 px height delta vs. sibling dock-panel bodies because rc-dock's `.dock-panel` includes a tab-header bar while `.dock-vbox` does not. Loosen layout-test height tolerances accordingly (we use 60 px).
  - Pattern for panels that should auto-appear based on app state (Figure, Call Stack, Watches): derive their visibility inside a `useMemo` with only the minimal triggers (e.g. `figures.length`, `pausedLocation`, `watches.length`) — using the raw array/object would recompute on every unrelated render.
  - When constructing `LayoutData` objects, rc-dock's TS types strictly require each tab to include `title` + `content`, but at runtime with `loadTab` provided only `id` is needed. Use `({ id } as unknown as TabData)` to keep the JSON-style builder terse without suppressing all type-checking on the return value.
---

## 2026-04-11 - US-026 Drag tabs between docks
- rc-dock already supports drag-between-docks natively via its shared-group mechanism (all MatSlop tabs use `group: 'main'` from US-025), so this story was about wiring persistence + writing a regression test.
- Extended `LayoutConfig` in `src/main/appConfig.ts` with an optional `dockLayout?: unknown` field (serialized rc-dock `LayoutBase`). Plumbed through `src/preload/index.ts` and `src/renderer/env.d.ts` type definitions.
- `MatslopDockLayout.tsx`: added `savedDockLayout` + `onDockLayoutChange` props, plus a `DockLayout` ref. Initial state prefers the saved layout when present; visibility changes still rebuild from scratch via a prev-key guard. `onLayoutChange` fires `onDockLayoutChange` for interactive directions (filtered out `'update'`/`'active'`). Added test-only globals `__matslopDockMove`, `__matslopDockSaveLayout`, `__matslopDockGetTabPanelId`.
- `App.tsx`: loads `dockLayout` from `layoutGet()`, keeps it in both state and a ref, passes to MatslopDockLayout, and `handleDockLayoutChange` persists via `layoutSet`. `togglePanel` and `resetLayout` clear the saved dock layout since visibility changes rebuild the tree.
- **FIXED a latent test-isolation bug** in `src/main/appConfig.ts`: the `new Store()` call was evaluated at module-load time (ESM hoists imports above the `app.setPath('userData', ...)` statement in `src/main/index.ts`), so electron-store latched onto the real production user-data dir even when `MATSLOP_USER_DATA_DIR` was set for tests. Switched to a lazy `store$()` + Proxy wrapper so construction is deferred until after userData is overridden. Before the fix, tests were inheriting persisted dockLayout from earlier dev sessions.
- Added `tests/e2e/dock-drag.spec.ts` with 2 tests: (1) Workspace tab + Command Window tab start in different rc-dock panels, `__matslopDockMove('matslop-workspace','matslop-command-window','middle')` merges them into the same panel id; (2) The saved dock layout round-trips through the layout IPC containing both tab ids.
- Typecheck passes (baseline noise only). 8/8 layout + dock-drag e2e + 11/11 dock-layout unit tests pass.
- Files changed: src/main/appConfig.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/panels/MatslopDockLayout.tsx, src/renderer/App.tsx, tests/e2e/dock-drag.spec.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - rc-dock's `fixBoxData` merges a box-with-one-child back into its parent, and `fixLayoutData` runs on every layout reload (on mount AND after controlled `onLayoutChange`). When debugging "my layout tree looks wrong at runtime," remember rc-dock normalizes it.
  - rc-dock's `DockLayout` ref exposes `dockMove(source, target, direction)`, `saveLayout()`, `loadLayout()`, and `find(id)` — use these to programmatically drive layout changes in Playwright instead of synthesizing pointer events on the flaky DragDropDiv. Expose them via `window.__matslopX` test hooks installed in a `useEffect` inside the dock component.
  - rc-dock tabs in the post-fix tree have a `parent` field pointing to their PanelData — use `dock.find(tabId).parent.id` to get a stable panel id for "are these two tabs in the same panel?" assertions. `closest('.dock-panel')` in the DOM doesn't always work because rc-dock caches inactive tabs in a shared portal.
  - In a merged tab group, only the ACTIVE tab is rendered visibly — non-active siblings have `visibility: hidden`. Use `toHaveCount(1)` instead of `toBeVisible()` when asserting presence of merged tabs.
  - Test-isolation gotcha for Electron + electron-store + MATSLOP_USER_DATA_DIR: ESM hoists all `import` statements above top-level code, so any module that constructs a Store at module-load time latches onto the CURRENT userData path BEFORE `app.setPath('userData', ...)` runs. Fix by lazily constructing the store after first access (wrap in a Proxy if existing callers reference it as a const).
  - For renderer debugging in Playwright, `page.on('console', ...)` is registered too late to catch mount-time logs. Better: write debug info to a `window.__matslopX` global from the component initializer and read it from the test via `page.evaluate`.
---

## 2026-04-11 - US-027 Detach panel to separate OS window
- Added `panel:openDetached` / `panel:closeDetached` / `panel:_testDetachedList` IPC handlers in src/main/index.ts (alongside the existing US-012 plot detach). Main tracks `detachedPanelWindows: Map<tabId, BrowserWindow>` and sends `panel:redocked` to mainWindow on close.
- Preload bridge: added `panelOpenDetached`, `panelCloseDetached`, `onPanelRedocked`, `_testDetachedPanelList`. env.d.ts updated to match.
- `MatslopDockLayout.tsx`: `buildDockLayoutFromVisibility(vis, detached?)` now omits tabs whose id is in the detached set — center/right columns collapse cleanly when all their tabs are detached. Added `detachedPanels` + `onDetachTab` props. Layout rebuilds on detached-set changes (tracked alongside vis key).
- Tab title rendered as a `<span>` with `onContextMenu` to open a custom floating context menu; menu has a single "Detach to window" option that invokes `onDetachTab`. Menu dismissed on outside-click (bubble phase, so the button's own onClick still fires) and Escape.
- App.tsx holds `detachedPanels: Set<string>` state, `handleDetachTab` calls the IPC + optimistically omits the tab (rolls back on IPC failure), and a `useEffect` subscribes to `onPanelRedocked` to restore the tab on window close.
- `src/renderer/DetachedPanel.tsx` — new lightweight component rendered in the aux BrowserWindow for any `?detachedPanelId=...` query param. Shows panel title, shared Octave status via `octaveGetStatus`+`onOctaveStatusChanged`, a close/redock button, and a per-panel live view (workspace runs `whos`, file-browser lists `pwd`, call-stack runs `dbstack`, history reads the persisted log). Proves the detached window shares state with the main Octave process via the same preload bridge.
- `main.tsx` now routes: detachedFigureId → DetachedPlot (US-012), detachedPanelId → DetachedPanel (US-027), else App.
- Test hooks: `window.__matslopDetachPanelTab(id)`, `__matslopOpenTabContextMenu(id)`, `__matslopDetachedPanels`.
- Tests: tests/unit/dock-layout.test.ts gained 5 US-027 cases covering the detach/omit logic (all 16 dock-layout unit tests pass). tests/e2e/detached-panel.spec.ts covers the full detach→redock cycle for the Workspace panel + the context-menu path for File Browser. Both pass against the built app.
- Files changed: src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/App.tsx, src/renderer/main.tsx, src/renderer/DetachedPanel.tsx (new), src/renderer/panels/MatslopDockLayout.tsx, tests/unit/dock-layout.test.ts, tests/e2e/detached-panel.spec.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - To intercept clicks on a floating menu while still allowing the menu's own buttons to fire their React onClick handlers, register the outside-click dismiss listener in the BUBBLE phase (not capture). Capture-phase dismiss runs before React dispatches the synthetic click to the button, unmounts the menu, and the button's onClick never fires. Guard the dismiss by checking `menuEl.contains(e.target)`.
  - rc-dock's `TabData.title` is typed as `string` but accepts a `ReactElement` at runtime — cast `titleNode as unknown as string`. This lets you attach `onContextMenu` to tab headers without forking rc-dock.
  - To route a new auxiliary window class (beyond US-012's plot), add another branch to the `main.tsx` query-param router. Keep the detached component self-contained: only depend on `window.matslop.*` IPCs, not App.tsx state or React context. The aux window shares the preload bridge (and hence the main-process singletons) but has a separate React tree.
  - When spawning a BrowserWindow in main, always send a webContents event to mainWindow in the `closed` handler if the main renderer needs to reconcile state (same pattern as US-012's focus refocus + this story's `panel:redocked`).
  - For "omit-when-detached" layout rebuilds, reuse the existing visibility rebuild path in the dock component: track both keys in a ref and rebuild in the same useEffect. Saved dock layouts must be cleared on detach/redock the same way `togglePanel` clears them, or rc-dock will re-apply a stale tree that still contains the detached tab.
---

## 2026-04-11 - US-028 Layout presets
- Added pure helpers `src/renderer/editor/layoutPresets.ts` (4 built-in presets: Default/Debugger/Two-Column/Code-Only, captureLayoutPreset, parseLayoutPresetAction, validatePresetName). 20 vitest cases in tests/unit/layout-presets.test.ts.
- Extended `src/main/appConfig.ts` with `layoutPresets: Record<string, StoredLayoutPreset>` and CRUD wrappers (getLayoutPresets/getLayoutPreset/saveLayoutPreset/deleteLayoutPreset/listLayoutPresetNames) backed by electron-store. Cap at 50 presets.
- New IPCs `layoutPresets:list/get/save/delete` in `src/main/index.ts`. Save/delete call rebuildMenu() so the View → Layouts submenu updates immediately.
- `src/main/appMenu.ts` gains a `View → Layouts` submenu listing the 4 built-ins, then custom presets (each with Apply/Delete), then "Save Current as Preset...". buildAppMenu() now takes a `customPresetNames` array.
- Preload bridge + env.d.ts extended with `layoutPresetsList/Get/Save/Delete`.
- New `src/renderer/dialogs/SavePresetDialog.tsx` — modal with live validation, Enter submit, Escape cancel, and an overwrite warning when the name collides with an existing custom preset.
- `src/renderer/App.tsx`: added `applyLayoutPreset/applyBuiltinPreset/applyCustomPreset/deleteCustomPreset/handleSaveLayoutPreset` helpers. Menu switch forwards `layoutPreset:builtin:<id>`, `layoutPreset:custom:<name>`, `layoutPreset:delete:<name>` and `saveLayoutPreset` actions. Reset Layout is now an alias for applying the Default built-in. Moved visibilityRef/panelSizesRef to above the preset helpers so the callbacks can snapshot live state without dep-array churn.
- E2E coverage: `tests/e2e/layout-presets.spec.ts` — 6 tests (Code-Only hides everything, Two-Column keeps command window, Debugger adds history tab, Reset/Default round-trip, save→custom preset→apply→delete flow, validation error messages). All pass.
- Files changed: src/renderer/editor/layoutPresets.ts (new), src/renderer/dialogs/SavePresetDialog.tsx (new), src/main/appConfig.ts, src/main/appMenu.ts, src/main/index.ts, src/preload/index.ts, src/renderer/env.d.ts, src/renderer/App.tsx, tests/unit/layout-presets.test.ts (new), tests/e2e/layout-presets.spec.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - To add a new View menu submenu whose entries depend on store state: extend `buildAppMenu(mainWindow, recentFiles, ...)` with a new params, then call `rebuildMenu()` from every IPC handler that mutates that state. Pattern is the same as recent-files.
  - `parseLayoutPresetAction()` is an example of putting the action-string schema next to the consumer. Menu actions that encode a payload in the string (e.g. `layoutPreset:custom:<name>`) benefit from a pure parser + matching builder functions — makes them round-trip-testable without needing a live menu.
  - `captureLayoutPreset()` deep-copies visibility/sizes to insulate the stored snapshot from subsequent mutation — important because React state objects from setState callbacks can be reused across renders.
  - `visibilityRef` / `panelSizesRef` pattern (already used by handleDockLayoutChange) is the right place to read "current" state from menu-driven callbacks without forcing dep-array re-binds on every visibility change.
---
