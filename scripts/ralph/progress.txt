# Ralph Progress Log
Started: Sat Apr 11 06:46:53 PM EDT 2026
---

## Codebase Patterns
- download-octave.js: per-platform pinned versions (WIN_OCTAVE_VERSION, MAC_OCTAVE_VERSION). Windows ships from ftp.gnu.org/gnu/octave/windows (NOT ftpmirror.gnu.org — that redirector 502s). macOS is pinned to octave-app v9.2 (release ceiling). Linux is intentionally NOT bundled — no maintained AppImage source; the script exits cleanly with package-manager guidance and src/main/octaveConfig.ts handles PATH lookup at runtime.
- Tests that assert download-octave.js content live at tests/unit/download-octave-{linux,mac}.test.ts and read the script as a string — any URL or constant change needs matching regex updates there.
- octaveConfig.ts retains its legacy squashfs-root branch as a fallback for users who manually drop an AppImage into resources/octave/, even though the download script no longer populates it.
- Renderer-side octaveExecute interception: wrap `window.matslop.octaveExecute` once from src/renderer/main.tsx (before any component mounts) via an idempotent shim. Avoids touching ~30 call sites across panels/dialogs. `src/renderer/octaveBusyTracker.ts` is a working example — it ref-counts in-flight executes for the "Running…" status-bar indicator.
- Use `vi.advanceTimersByTimeAsync` (not the sync variant) when code under test awaits promises between timer fires — the async version drains microtasks between ticks.
- To unit-test OctaveProcessManager without a real child, mock `child_process` at module level (`vi.mock('child_process', () => ({ spawn: vi.fn(() => new FakeChildProcess()) }))`) then `await import('.../octaveProcess')` AFTER the mock. FakeChildProcess extends EventEmitter with `stdin.write`, `stdout`, `stderr` emitters; emit `___MATSLOP_CMD_DONE___\n` on stdout to complete the init handshake.
- OctaveProcessManager.interrupt() (SIGINT, keeps process) and .stop() (writes 'exit' then SIGKILLs after 2s) are NOT interchangeable. The editor Stop button calls interrupt(); only full shutdown/restart uses stop().
- Editor Run (F5) flows: TabbedEditor → EditorToolbar onRun → EditorPanel.handleRun (auto-save or Save As) → App.handleRunScript → buildRunScriptCommand → setPendingCommand → CommandWindow. Use source('<abs path>') not run() — source surfaces top-level output through the Command Window pipeline.
- Octave rule used by isFunctionOnlyFile: a file is a function file iff the first non-blank, non-comment statement is the `function` keyword (word-bounded). Lines before that may be blank, `%`/`#` line comments, or `%{`/`%}` block comments. Extract pure helpers like this into src/renderer/editor/*.ts so they're unit-testable without Monaco/Electron.

---

## 2026-04-11 - US-S01
- Fixed broken URLs in scripts/download-octave.js discovered during real-Octave validation.
- Changes:
  - Windows URL switched from ftpmirror.gnu.org → ftp.gnu.org/gnu/octave/windows (was returning 502).
  - macOS pinned to octave-app v9.2 (was v9.4.0 — that release does not exist).
  - Linux bundling removed — the ryanjdillon/octave-appimage repo doesn't exist. Script now short-circuits for --platform=linux with a clear package-manager install guide and relies on runtime PATH lookup.
  - Introduced per-platform constants WIN_OCTAVE_VERSION / MAC_OCTAVE_VERSION (replaces the single OCTAVE_VERSION).
  - extractedDir for Windows corrected to `octave-${WIN_OCTAVE_VERSION}-w64` (GNU's actual archive layout).
- Files changed:
  - scripts/download-octave.js (URLs, versions, linux fallback, removed AppImage extract branch)
  - tests/unit/download-octave-linux.test.ts (rewritten around the unbundled fallback contract)
  - tests/unit/download-octave-mac.test.ts (added assertions pinning v9.2 ceiling and ftp.gnu.org)
  - scripts/ralph/prd.json (US-S01 passes: true)
- Verification: typecheck clean, all 523 unit tests pass, `node scripts/download-octave.js --platform=linux` exits 0 with friendly message and no HTTP stacktrace.
- **Learnings for future iterations:**
  - When changing download-octave.js, remember tests match script content via regex — keep comment phrasing out of assertion paths (I tripped a ftpmirror.gnu.org assertion because I referenced it in a comment).
  - Per-platform version constants are clearer than a single global for packaging scripts where upstream sources release at different cadences.
  - The linux fallback path is exercised by an execFileSync e2e assertion in tests/unit/download-octave-linux.test.ts — any future linux branch change will be caught by that test.
---

## 2026-04-11 - US-S02
- Added ref-counted "Octave is running" busy tracker in the renderer. Every window.matslop.octaveExecute IPC goes through it via a wrapOctaveExecute(bridge) shim installed from main.tsx at startup (idempotent, HMR-safe).
- Status bar now flips to "Running…" only after 250ms of sustained in-flight activity — sub-threshold commands stay silent (no flicker). Nested/overlapping executes share a single ref count; we only return to Ready when the LAST execute settles. Fast rejects still decrement via try/finally.
- Files changed:
  - src/renderer/octaveBusyTracker.ts (new module — singleton + wrapper)
  - src/renderer/main.tsx (install wrapper before App mounts)
  - src/renderer/App.tsx (subscribe to tracker, pass `running` to StatusBar)
  - src/renderer/panels/StatusBar.tsx (new `running` prop; label becomes "Running…"; data-running attr for tests)
  - tests/unit/octave-busy-tracker.test.ts (11 tests — fast-command no-flicker, threshold crossing, nested ref counting, rejection decrement, idempotent wrap)
  - scripts/ralph/prd.json (US-S02 passes: true)
- Verification: typecheck clean, 534 tests pass (11 new). Lint errors are pre-existing (react-hooks rule not configured) and unrelated.
- **Learnings for future iterations:**
  - Wrap-the-bridge in main.tsx is the cleanest way to intercept every octaveExecute without touching the ~30 call sites scattered across panels/dialogs.
  - OctaveProcessManager already emits busy/ready on its 'status' event per command — but that's per-IPC and flickers rapidly. The renderer-side ref-counted tracker is the right layer for a user-facing "is anything happening?" indicator.
  - Use `vi.advanceTimersByTimeAsync` (not `advanceTimersByTime`) when the code under test awaits promises between timer fires — the async variant drains the microtask queue between ticks.
  - StatusBar's `engine-status` testid now also exposes `data-running` for future e2e assertions.
---

## 2026-04-11 - US-S03
- Verified the editor Stop button interrupts long-running commands via SIGINT and does NOT tear down the Octave process (workspace state preserved).
- Flow: EditorToolbar Stop → App.handleStop → window.matslop.octaveInterrupt → ipc 'octave:interrupt' → OctaveProcessManager.interrupt() → process.kill('SIGINT'). All wiring already existed from prior cycles — this story was a verification + test story.
- Added tests/unit/octave-interrupt.test.ts (4 tests) that mock child_process.spawn with a FakeChildProcess and assert:
  - interrupt() sends exactly one SIGINT (no SIGKILL, no SIGTERM) when a command is in flight
  - isRunning() stays true and status stays 'busy' until Octave responds — the process is not torn down
  - when the delimiter eventually arrives the pending executeCommand resolves with the 'error: interrupted' stderr and status returns to 'ready'
  - interrupt() is a safe no-op when never started, and when idle (status !== 'busy')
  - handleStop click path reaches window.matslop.octaveInterrupt exactly once per click
- Files changed:
  - tests/unit/octave-interrupt.test.ts (new, 4 tests)
  - scripts/ralph/prd.json (US-S03 passes: true)
- Verification: typecheck clean, 538 tests pass (was 534). Browser verification skipped — no MCP dev-browser tool active in this iteration; wiring is covered by the unit test against the real OctaveProcessManager implementation.
- **Learnings for future iterations:**
  - To unit-test OctaveProcessManager without spawning real Octave: `vi.mock('child_process', () => ({ spawn: vi.fn(() => new FakeChildProcess()) }))` where FakeChildProcess extends EventEmitter and exposes stdin.write / stdout / stderr event emitters. Then import OctaveProcessManager AFTER the mock with `await import(...)` so the manager picks up the stubbed spawn.
  - The init-script handshake must be completed by emitting `___MATSLOP_CMD_DONE___\n` on stdout before the manager will accept new commands (status flips from 'busy' to 'ready').
  - interrupt() is intentionally different from stop(): interrupt only sends SIGINT (Octave handles cleanup), stop() writes 'exit' and SIGKILLs after 2s. Never confuse them — the Stop BUTTON calls interrupt(), not stop().
  - Caveat: `debug_on_interrupt(true)` is set in the init script (octaveProcess.ts:126). For commands running inside a user script/function, SIGINT may drop into the debugger instead of cancelling. Top-level prompt commands still cancel normally. If future polish needs a stricter "cancel no matter what" Stop, consider queuing a `dbquit` after SIGINT.
---

## 2026-04-11 - US-S04
- Unified formatValuePreview in WorkspacePanel.tsx and exported it for testing.
- New contract:
  - Scalars: 6 sig digits via toPrecision(6), exponential for |n|<1e-4 or |n|>=1e6, trailing zeros trimmed (3.14159, 1e-5, 42). Logical scalars → true/false. Zero → "0". Inf/NaN pass through.
  - Strings (char/string class): strip surrounding quotes, truncate at 20 visible chars + "...".
  - Non-scalars (struct, cell, function_handle, any matrix): uniform "[RxC class]" — even at size 1x1 for non-numeric classes.
- Files changed:
  - src/renderer/panels/WorkspacePanel.tsx (formatValuePreview rewrite + export + helpers extractValueToken/truncateString/formatNumber + nonScalarClasses guard)
  - tests/unit/format-value-preview.test.ts (new, 21 tests across scalar/string/matrix/struct/cell cases)
  - scripts/ralph/prd.json (US-S04 passes: true)
- Verification: typecheck clean, 559 tests pass (was 538, +21 new).
- **Learnings for future iterations:**
  - JS `Number.prototype.toPrecision(6)` only flips to exponential when |n|<1e-6 by default — if you want "1e-5" instead of "0.00001" you must branch on magnitude and call `toExponential(5)` manually.
  - Order matters in the workspace preview formatter: check string classes FIRST (a 1x3 char is not a "scalar"), then non-scalar object classes (struct/cell — even 1x1 structs aren't numbers), then size === '1x1' for real numeric scalars, then the matrix fallback. Getting this order wrong makes a 1x1 struct render as a bogus number.
  - formatValuePreview is now exported from WorkspacePanel.tsx alongside parseWhosOutput — both are pure and unit-tested in tests/unit/.
---

## 2026-04-11 - US-S05
- Editor Run (F5) now sources the whole open script via `source('<abs path>')` so top-level output flows into the Command Window, and short-circuits function-only files with a visible warning banner instead of a silent no-op.
- Added new pure module src/renderer/editor/functionFileDetection.ts exporting:
  - `isFunctionOnlyFile(content)` — Octave rule: first non-blank, non-comment statement begins with the `function` keyword (word-bounded). Handles blank lines, `%`/`#` line comments, `%{`/`%}` block comments, CRLF, and guards against `function_handle`/`functional` false positives.
  - `buildRunScriptCommand(filePath, dirPath)` — returns `{ command: "cd('<dir>'); source('<abs path>')", display: "source('<file>')" }`, with single quotes correctly doubled for Octave string escaping. Exported so App.tsx and the tests share the same implementation.
- EditorPanel.handleRun now calls isFunctionOnlyFile on the *live* buffer content before auto-save. Function-only or empty/comment-only buffers set a new `runWarning` state that renders a dismissible banner ("This file only defines function(s); nothing to run.") above the editor. Unsaved buffers still trigger Save As first.
- App.handleRunScript switched from `cd(...); run('file')` to the shared buildRunScriptCommand helper.
- Files changed:
  - src/renderer/editor/functionFileDetection.ts (new)
  - src/renderer/panels/EditorPanel.tsx (import helper, runWarning state, handleRun short-circuit, banner markup)
  - src/renderer/App.tsx (handleRunScript uses buildRunScriptCommand, import added)
  - src/renderer/styles.css (.editor-run-warning + dismiss button styles — cloned the existing warning-banner look)
  - tests/unit/function-file-detection.test.ts (new, 21 tests: 9 function-only cases + 8 script cases + 4 command-builder cases)
  - scripts/ralph/prd.json (US-S05 passes: true, notes filled in)
- Verification: typecheck clean, all 580 tests pass (was 559, +21 new). Browser verification deferred — no MCP dev-browser active in this iteration; the logic is covered by pure unit tests against the same helpers the renderer calls.
- **Learnings for future iterations:**
  - Octave's `source('<abs path>')` executes top-level code in the caller's workspace and streams output back through stdout — this is what the Command Window pipeline needs. `run('<file>')` internally cds then executes but does not reliably surface output when called from another command. Use source.
  - When adding a new editor-level banner, clone the `.edit-continue-banner` styles in styles.css rather than inventing a new look — the app already has three near-identical yellow warning banners (.edit-continue-banner, .octave-not-found-banner, .editor-run-warning) and they should stay visually consistent.
  - Pure exported helpers in src/renderer/editor/ are the right place for anything that needs unit coverage without Monaco/Electron. Don't bury logic inside useCallback bodies — extract, export, test, re-import.
  - The `function` keyword detection needs a word boundary (`/^function\b/`) — otherwise `function_handle = ...` or `functional = 1` would be misclassified as a function file.
---
