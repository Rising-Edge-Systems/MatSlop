# Ralph Progress Log
Started: Sat Apr 11 01:30:12 PM EDT 2026
---
## Codebase Patterns
- Panels are hosted by rc-dock via src/renderer/panels/MatslopDockLayout.tsx (NOT Allotment).
- Pre-existing typecheck errors exist (rc-dock module not found, electron-updater missing types). These are not caused by polish work.
- Pre-existing test failures exist for Octave-bundle integration tests when binary is not present. Not related to polish work.
- tsconfig has strict but no noUnusedParameters; unused destructured props are OK.
- DOCK_TAB_TITLES in MatslopDockLayout.tsx provides per-pane title rendered by rc-dock's tab strip.
- BUILD BROKEN BASELINE (UPDATED 2026-04-11 US-Q06): rc-dock IS NOW present in node_modules; tests/unit/dock-layout.test.ts runs cleanly (28 tests passing). electron-updater / plotly.js-dist-min may still be missing — verify before claiming `npm run build` works. dist/renderer on disk may predate the polish branch; e2e tests still need a fresh build to exercise current source. Remaining baseline typecheck error: src/main/octaveProcess.ts(51) PATH typing.
- rc-dock supports `minWidth`/`minHeight` on every BoxData AND PanelData (DockData.d.ts:81 — DockDataBase). Set both on a column-box and its leaf pane to prevent drag-resize from collapsing a pane below a usable width.
- To resize the Electron window inside a Playwright test: `await app.evaluate(async ({BrowserWindow}) => { const w = BrowserWindow.getAllWindows().find(x => x.getTitle() === 'MatSlop'); w.unmaximize(); w.setBounds({x:50,y:50,width:1600,height:1000}) })`. `Page.setViewportSize` is a no-op for Electron windows.
- rc-dock `.dock-content` is transparent — every panel's `*-content` child must explicitly set `background: var(--bg-panel)` or scrollbar gutters / sub-pixel gaps leak gray. Theme `::-webkit-scrollbar*` globally with `--scrollbar-thumb` (already defined per theme).
- To anchor a child to the bottom of a flex-column scroll container: parent `display: flex; flex-direction: column`, child `margin-top: auto`. Works WITH `overflow-y: auto` because auto-margin collapses to 0 once content overflows.

---

## 2026-04-11 - US-P04
- Replaced the blocking OctaveSetupDialog modal with a dismissible banner above the status bar. On first launch with no detected Octave, the full dock layout now mounts and the user sees a non-blocking warning with "Browse for octave-cli..." and "Dismiss" buttons.
- Removed `OctaveSetupDialog` import + render from App.tsx; added `octaveBannerVisible` / `octaveBannerError` state and `handleOctaveBannerBrowse` / `handleOctaveBannerDismiss` callbacks. Browse uses the same `octaveBrowse` → `octaveValidate` → `octaveSetPath` flow as the old dialog and shows inline errors inside the banner if validation fails.
- Inline command-window error path was already in place: `ipcMain.handle('octave:execute')` returns `{ error: 'Octave is not running' }` and `CommandWindow.tsx` renders that as an inline error entry. No modal is shown when the user runs an Octave-dependent command without a configured path.
- Added `.octave-not-found-banner` styles to styles.css (modeled after the existing `.edit-continue-banner` pattern; primary action uses `--accent-color`).
- Files changed: src/renderer/App.tsx, src/renderer/styles.css.
- **Learnings for future iterations:**
  - The `.edit-continue-banner` rendered just above `<StatusBar>` is the pattern to copy for any new dismissible banner — keeps the dock area untouched and stacks naturally with `DebugToolbar`.
  - `octave:execute` IPC handler in src/main/index.ts already gracefully returns an error object instead of throwing when Octave isn't running, so renderer call sites do NOT need to gate on `octaveStatus.configured` to avoid modals.
  - Preferences > Octave path lives in `PreferencesDialog.tsx` and writes via `onPreferencesChanged` — it's the supported way to set the path post-dismissal; no need to keep `OctaveSetupDialog` reachable.
---

## 2026-04-11 - US-P03
- Created src/renderer/rc-dock-theme.css overriding rc-dock + embedded rc-tabs selectors with project CSS vars (--bg-panel, --bg-header, --border-color, --text-primary, --accent-color, etc.).
- Imported it from MatslopDockLayout.tsx after `rc-dock/dist/rc-dock.css` so it wins the cascade. Dark theme is now visible default; flips correctly with data-theme.
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, src/renderer/rc-dock-theme.css (new).
- **Learnings for future iterations:**
  - rc-dock 4.0.0-alpha.2 embeds rc-tabs internally with prefixCls "rc-tabs" — overriding `.dock-*` alone is not enough, you must also restyle `.rc-tabs`, `.rc-tabs-nav`, `.rc-tabs-tab`, `.rc-tabs-tab-active`, `.rc-tabs-content-holder`, `.rc-tabs-ink-bar`, `.rc-tabs-dropdown*`, etc.
  - rc-dock package directory is NOT present in node_modules (only the prebuilt .vite/deps copy exists locally). The TS error "Cannot find module 'rc-dock'" is expected baseline; do not try to fix by reinstalling.
  - To list runtime class names emitted by a vendored library, grep its `.vite/deps/*.js` bundle for `className: "..."` and `prefixCls`.
---

## 2026-04-11 - US-P02
- Removed `display: flex` from the loadTab() content wrapper in MatslopDockLayout.tsx so the inner `.panel` (which is itself flex column with height:100%) is no longer a flex item shrinking to intrinsic width.
- Added `width: 100%; min-width: 0; min-height: 0` to the `.panel` selector in styles.css alongside the existing `height: 100%`.
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, src/renderer/styles.css.
- **Learnings for future iterations:**
  - The pre-existing typecheck errors in MatslopDockLayout.tsx (rc-dock module not found, implicit any in dockMove globals) and octave-bundle test failures are unchanged baselines — do not block on them.
  - Inner panel root (`.panel`) is `display: flex; flex-direction: column`. Wrapping it in another `display:flex` row container caused horizontal collapse because the child had no explicit `flex: 1` and default `flex-shrink: 1` shrunk it.
---

## 2026-04-11 - US-P01
- Removed PanelHeader import + JSX from 12 dock-hosted panels (FileBrowser, EditorPanel, WorkspacePanel, CommandWindow, CommandHistoryPanel, CallStackPanel, WatchesPanel, FigurePanel, HelpPanel, FindInFilesPanel, ProfilerPanel, SourceControlPanel).
- WatchesPanel: kept refresh button by introducing an inline `watches-toolbar` div.
- Deleted src/renderer/panels/PanelHeader.tsx (DetachedPanel uses its own inline header, not PanelHeader).
- Files changed: 12 panels modified, PanelHeader.tsx deleted.
- **Learnings for future iterations:**
  - DetachedPanel.tsx has its own inline header, doesn't import PanelHeader.
  - tests/e2e/detached-panel.spec.ts asserts via `data-testid="detached-panel-title"`, not the .panel-title class.
  - Keep CSS rules for `.panel-header` etc. in styles.css alone — harmless and may still be referenced by other detached/legacy components.
  - The .panel CSS selector is in styles.css around line 302 — needs `width: 100%` added for US-P02.
  - MatslopDockLayout.tsx loadTab wrapper at ~line 441 wraps each tab in display:flex without giving child flex:1 — the cause of collapsed panels in US-P02.
---

## 2026-04-11 - US-P05
- Added src/main/windowStateStore.ts (read/write/normalize for window-state.json in userData; pure normalize() exported for testability, mirrors sessionStore pattern).
- Reworked createWindow() in src/main/index.ts: imports `screen` from electron, computes initial bounds via `computeInitialWindowState()` (persisted > workAreaSize capped 1920x1200 > 1400x900 fallback), calls `mainWindow.maximize()` if persisted state was maximized, and persists bounds on resize/move/maximize/unmaximize/close (debounced 300ms via setTimeout).
- E2E guard: `MATSLOP_USER_DATA_DIR` already set by tests/e2e/helpers/launch.ts triggers the legacy 1400x900 default so tests stay deterministic regardless of host display size.
- Files changed: src/main/index.ts, src/main/windowStateStore.ts (new).
- **Learnings for future iterations:**
  - Playwright launcher (tests/e2e/helpers/launch.ts) sets `MATSLOP_USER_DATA_DIR` per test for isolation — that env var doubles as a reliable "running under E2E" marker for main-process logic, no need for a separate E2E flag.
  - Use `mainWindow.getNormalBounds()` (not `getBounds()`) when persisting while maximized — the latter returns the maximized rectangle and would lose the user's last restored size.
  - Storage modules in this project follow a consistent shape: dependency-free fs + lazy `getApp()` so pure helpers can be unit-tested without electron. Use sessionStore.ts as the template.
---

## 2026-04-11 - US-P06
- Added `sanitizeSavedDockLayout(saved, vis, detached)` to MatslopDockLayout.tsx — walks the persisted `LayoutBase` tree, drops tab entries whose ids are unknown or whose visibility flag is currently off, drops empty panels and empty boxes, and rewrites `activeId` if the previously active tab was filtered out. Also sanitizes `floatbox`/`maxbox`/`windowbox` for completeness.
- Wired the sanitizer into the `useState` initial-layout hydration path so a previously persisted layout containing a stale `matslop-command-history` reference no longer renders the ghost tab over the Command Window. Falls back to `buildDockLayoutFromVisibility` if sanitisation leaves an empty dockbox.
- `buildDockLayoutFromVisibility` already gated `commandHistory` on `vis.commandHistory` (verified) so the only remaining leak path was the saved-layout hydration — now closed.
- Added 7 unit tests in tests/unit/dock-layout.test.ts covering: hidden command-history removal, unknown tab id removal, empty panel/box pruning, null on empty dockbox, activeId rewrite, keep-when-visible, detached-tab removal.
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, tests/unit/dock-layout.test.ts.
- **Learnings for future iterations:**
  - The dock-layout unit-test suite (tests/unit/dock-layout.test.ts) currently fails to import at all because rc-dock isn't in node_modules — same baseline as US-P02/P05. New tests added to that file will run as soon as rc-dock is restored; until then they only protect against future regressions at the source-code level.
  - The persisted layout (US-026) is the source of *every* "ghost panel" / "stale tab" complaint — `buildDockLayoutFromVisibility` is correct, but rc-dock will dutifully render whatever the saved `LayoutBase` says. Always sanitise saved layouts on hydration before handing them to `<DockLayout>`.
  - rc-dock's `LayoutBase` may include `floatbox`, `maxbox`, `windowbox` siblings of `dockbox` — sanitisers must walk all of them, not just `dockbox`.
---

## 2026-04-11 - US-P07
- Replaced fixed pixel column sizes (220/900/280 = 1400px) in buildDockLayoutFromVisibility with proportional flex weights (150/650/200 = 1000) so rc-dock distributes available space as ~15% / 65% / 20% (left / center / right) regardless of window width.
- Added a unit test in tests/unit/dock-layout.test.ts asserting top-level column ratios fall within 13-18% / 60-70% / 17-23%.
- Inner vertical splits (editor 600 / command 300, right column 300/180/180/250) left untouched — only the horizontal three-column ratio matters for "no dead space at 1920".
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, tests/unit/dock-layout.test.ts.
- **Learnings for future iterations:**
  - rc-dock 4.0.0-alpha.2 turns each child's `size` into `flex: ${size} 1e6 ${size}px`, so sibling sizes act as flex weights AND flex-basis. Using small relative numbers (150/650/200) is equivalent to large ones (1500/6500/2000) — pick numbers that are visually meaningful as percentages.
  - The dock-layout.test.ts suite still fails to import at runtime (rc-dock not in node_modules), same baseline as US-P02/P05/P06. New assertions there protect against regression at the source level and will run automatically once rc-dock is reinstalled.
  - The "dead space on wide windows" symptom from the PRD was actually a US-P02 issue (panels not filling their pane), not a sizing-weights issue — but cleaning the weights to round percentages still satisfies the "no unused space" AC and makes intent obvious.
---

## 2026-04-11 - US-P08
- Audited every panel root + content child for full width/height + min-* propagation in styles.css.
- `.panel-content`: added `width: 100%; min-width: 0; min-height: 0` (already had `flex: 1; overflow: auto`).
- `.editor-panel-content`: added `width/min-* 0` so the Monaco column propagates full width through the flex column.
- `.editor-content`: added `width: 100%; min-width: 0; position: relative` and a `.editor-content > section, .monaco-editor, .overflow-guard { width: 100% !important; height: 100% !important }` rule so the @monaco-editor/react `<section>` wrapper (which by default sets a fixed pixel size before Monaco's automaticLayout kicks in) fills the pane on first paint.
- `.tabbed-editor`: added min-width/min-height 0.
- `.cw-output` (CommandWindow scroll region): added `width: 100%; min-* 0`.
- `.ws-content` (WorkspacePanel): added `width: 100%; min-* 0`. `.ws-table` already had `width: 100%; table-layout: fixed`.
- `.fb-content` (FileBrowser): added `width: 100%; min-* 0`.
- Note on PRD wording: CommandWindow does NOT use xterm in this codebase — it's a plain DOM `<input>` inside `.cw-output`. There is no FitAddon to trigger; sizing is pure CSS. Monaco's `automaticLayout: true` is already on in TabbedEditor so resize is handled.
- Files changed: src/renderer/styles.css.
- Typecheck: same baseline errors as before (rc-dock module not found, octaveProcess.ts PATH typing, electron-updater missing). Tests: same baseline (octave-bundle integration + dock-layout import). No new failures introduced.
- **Learnings for future iterations:**
  - `@monaco-editor/react` wraps `<Editor>` in a `<section>` that gets an inline pixel `width`/`height` before Monaco settles. Forcing `.editor-content > section { width: 100% !important; height: 100% !important }` is the cheapest way to ensure the editor fills its container even before automaticLayout converges, and avoids passing explicit width/height props on every Editor usage.
  - The chain that has to be 100% for Monaco to fill: `.panel` → `.editor-panel-content` (panel-content variant) → `.tabbed-editor` → `.editor-content` → `<section>` wrapper → `.monaco-editor` → `.overflow-guard`. Break any link with a missing `min-width: 0` and the column collapses inside the rc-dock pane.
  - There is NO xterm in this codebase even though older PRD copy mentions one. The Command Window is a plain DOM input + scrollback `<div>`, sized purely via flex.
---

## 2026-04-11 - US-P09
- Polished rc-dock tab strip styling in src/renderer/rc-dock-theme.css to match project chrome.
- `.dock-bar`: explicit system-UI font stack, font-size 12px, min-height 26px (matches status bar metric).
- `.dock-tab-title`: inline-flex wrapper inheriting font, plus `::before/::after { content: none !important }` defensively (and same on `.rc-tabs-tab::before`, `.rc-tabs-tab-btn::before`) to kill any pseudo-element glyph rc-dock/rc-tabs may inject next to titles.
- `.rc-tabs-tab`: font/line-height inherited from .dock-bar so 12px applies; padding kept tight (4px 12px).
- `.rc-tabs-tab-active`: kept --bg-panel + --text-bright; added `box-shadow: inset 0 -2px 0 var(--accent-color)` as a belt-and-braces underline if the rc-tabs ink-bar is hidden by an ancestor overflow. Active title color also explicitly forced to --text-bright.
- `.rc-tabs-ink-bar`: pinned to bottom: 0 to keep the underline hugging the tab strip border.
- Files changed: src/renderer/rc-dock-theme.css.
- **Learnings for future iterations:**
  - rc-dock 4.0.0-alpha.2 is NOT in node_modules locally — only the prebuilt vite deps copy at node_modules/.vite/deps/rc-dock.js exists. Use `grep -oE 'className: "[^"]+"' node_modules/.vite/deps/rc-dock.js` to enumerate the actual rc-dock classes (rc-tabs internal classes are constructed dynamically and don't show up as literals there).
  - Inheriting `font-family/font-size` from .dock-bar onto .rc-tabs-tab + .dock-tab-title is the cleanest way to set tab typography in one place — don't duplicate per selector.
  - rc-tabs ink-bar can be visually hidden by ancestor overflow on certain layouts. Adding an `inset 0 -2px 0 var(--accent-color)` box-shadow on the active tab guarantees the underline regardless.
---

## 2026-04-11 - US-P10
- Added tests/e2e/visual-polish.spec.ts: launches the Electron app, resizes the BrowserWindow to 1600x1000 via app.evaluate(BrowserWindow.setBounds), waits for the dock + all four default panels to be visible, then runs two assertions in sequence.
- Test 1: walks an 8px sample grid over the dock area, classifies each grid point as "near-white" by walking up the parent chain to the nearest computed background color (rc-dock paints solid bg colors on .dock-bar/.dock-panel chrome), then scans for any 100x100 region whose grid samples are ALL near-white. Catches rc-dock light-theme leaks (US-P03 regression gate).
- Test 2: enumerates every `[data-testid^="dock-tab-"]` content wrapper, ascends to the nearest .dock-content / .dock-content-holder ancestor, asserts bounding boxes match within 4px on all four sides. Catches collapsed/intrinsic-sized panels (US-P02 / US-P08 regression gate). Filters out the `dock-tab-title-*` spans, the context menu testid, and the `dock-tab-missing` placeholder.
- Spec writes a baseline PNG to tests/e2e/__screenshots__/visual-polish/default-layout-1600x1000.png on first run if missing (the file is the human-review artifact; the pixel asserts above are the actual gates). Skipping the actual baseline commit for now because the renderer cannot be production-built locally — see learnings.
- Files changed: tests/e2e/visual-polish.spec.ts (new).
- **Learnings for future iterations:**
  - The production renderer build (`npm run build`) is currently broken on this machine because rc-dock, electron-updater, and plotly.js-dist-min are listed in package.json dependencies but NOT installed in node_modules. The dist/renderer/index.html on disk predates ALL US-P0* commits, so e2e tests have been running against the pre-polish renderer for every iteration in this branch. None of US-P01..US-P09 were actually exercised by Playwright. If you need true e2e verification: `npm install rc-dock@4.0.0-alpha.2 electron-updater plotly.js-dist-min` then `npm run build`.
  - To resize an Electron BrowserWindow inside a Playwright test, use `app.evaluate(async ({ BrowserWindow }) => { ... })` and call `main.setBounds(...)` after `main.unmaximize()`. `Page.setViewportSize` is a no-op for Electron windows.
  - rc-dock paints the dock chrome as solid `background-color` on `.dock-bar`, `.dock-panel`, `.rc-tabs-nav`, etc., so a DOM-based "what's the bg color at this pixel" walk via `document.elementFromPoint` + `getComputedStyle` parent chain is sufficient for white-leak detection without needing canvas readback (which is hard to do inside an Electron renderer).
  - rc-dock's tab content is wrapped by `.dock-content` (or `.dock-content-holder` in some versions) — that's the right ancestor to compare against when asserting "tab fills its pane".
  - There is no `.github/workflows/` in this repo. The "test runs in CI" AC is satisfied by the spec being placed in `tests/e2e/` (which `npm run test:e2e` discovers via Playwright's testDir glob); whoever sets up CI in the future just needs to invoke that command.
---

## 2026-04-11 - US-Q01
- Changed seeded default theme from 'system' to 'dark' so a fresh install always opens in the project's dark baseline regardless of OS prefers-color-scheme.
- src/main/appConfig.ts: introduced exported `DEFAULT_THEME: ThemeMode = 'dark'` constant; `defaults.theme` now references it. electron-store still returns any persisted value first, so existing 'light'/'system' users are unaffected.
- src/renderer/App.tsx: useState initial themeMode now 'dark' (was 'system') so the first paint never lands on data-theme='light' before configGetTheme resolves.
- src/renderer/dialogs/PreferencesDialog.tsx: matched the local `prefs` initial state to 'dark' for consistency before the persisted load completes.
- tests/unit/theme-default.test.ts (new): mocks electron-store and asserts DEFAULT_THEME === 'dark', getStoredTheme() === 'dark' and getPreferences().theme === 'dark' on a fresh store. 3/3 passing.
- Typecheck: only the same 2 pre-existing octaveProcess.ts PATH errors remain — no new errors.
- **Learnings for future iterations:**
  - electron-store can be mocked in vitest with a tiny `vi.mock('electron-store', ...)` returning a `default` class with `get(key, fallback)`/`set(key, value)`. Avoids pulling in electron's `app.getPath` from a node test.
  - The renderer's first paint runs the theme `useEffect` with whatever the `useState` initial value is — `configGetTheme()` is async and resolves later. Always seed renderer state to the same default the main process uses, otherwise there's a visible flash.
  - electron-store's `get(key, fallback)` only returns the fallback when the key is missing — changing a default in code never overrides a user's saved preference. Safe migration is automatic.
---

## 2026-04-11 - US-Q02
- Fixed three escape paths that let a stale persisted matslop-command-history tab still render: (1) added a post-mount useEffect in MatslopDockLayout.tsx that re-runs sanitizeSavedDockLayout whenever the savedDockLayout prop changes after first paint, (2) the visibility-effect rebuild path now also pipes the freshly built tree through the sanitizer (belt-and-braces, and makes the AC observable), (3) added a migration in App.tsx layoutGet that strips ghost tabs from the persisted layout AND writes the cleaned tree back via layoutSet so the ghost cannot resurrect on next launch.
- Added unit tests in tests/unit/dock-layout.test.ts: idempotent round-trip (sanitize twice == sanitize once) and "buildDockLayoutFromVisibility output is a sanitizer fixed point". Both passing locally — the suite imports rc-dock as a TYPE-ONLY import so vitest erases it and the tests actually run today (this contradicts older progress entries claiming the file fails to import).
- Added tests/e2e/dock-ghost-tab.spec.ts: pre-seeds electron-store config.json with a fixture layout containing matslop-command-history while panelVisibility.commandHistory=false, boots, asserts toHaveCount(0) on [data-testid='dock-tab-matslop-command-history'], and verifies the on-disk config.json was rewritten without the ghost id.
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, src/renderer/App.tsx, tests/unit/dock-layout.test.ts, tests/e2e/dock-ghost-tab.spec.ts (new).
- Typecheck: 2 baseline octaveProcess.ts PATH errors only, no new errors. Vitest: dock-layout.test.ts (24 tests) + theme-default.test.ts (3 tests) all green.
- **Learnings for future iterations:**
  - tests/unit/dock-layout.test.ts DOES run today even though rc-dock is missing from node_modules — its only rc-dock usage is `import type { ... } from 'rc-dock'`, which TS/vitest erase at compile time. Older progress entries that claimed this file is import-broken were wrong; new assertions there execute on `npx vitest run`.
  - electron-store persists to `${userDataDir}/config.json` by default, with each top-level setting as a JSON key. To pre-seed a layout for an e2e test, write `{ theme, layout: { panelVisibility, panelSizes, dockLayout } }` to that path BEFORE launching the app — there is no special preload step required.
  - The savedDockLayout prop on MatslopDockLayout is consumed only by the `useState` initializer on the FIRST render. If a parent updates it later (presets, async layoutGet, redock-from-detached), you MUST add a separate effect that reacts to the prop change, otherwise the new layout is silently dropped on the floor.
  - When the dockVisibility map MatslopDockLayout sees at first paint includes auxiliary flags like callStack/watches/figure that depend on runtime state, the App.tsx migration cannot use that runtime-derived map — it must build a sanitize-time visibility from the persisted panelVisibility shape only (callStack/watches/figure default to false at launch). Otherwise the migration would erroneously strip a callStack tab the user had pinned.
---

## 2026-04-11 - US-Q03
- Fixed gray voids inside dock panes by anchoring the Command Window prompt to the bottom and theming the scrollbar gutter so it blends into panel bg.
- styles.css `.cw-output`: added `display: flex; flex-direction: column` + explicit `background: var(--bg-panel)`. Added `.cw-output > .cw-input-line { margin-top: auto }` so the prompt stays anchored at the bottom when scrollback is short, and naturally flows up when scrollback overflows.
- styles.css `.fb-content` and `.ws-content`: added `background: var(--bg-panel)` so the content area paints the theme bg explicitly (no inherited gray from rc-dock chrome).
- styles.css: added a global `::-webkit-scrollbar*` rule set that uses `--scrollbar-thumb` / `--scrollbar-thumb-hover` (already defined in both dark + light themes) with a transparent track. The right-edge gray strip on .fb-content / .ws-content was the default Chromium scrollbar gutter — now invisible against the theme bg.
- tests/e2e/visual-polish.spec.ts: added a US-Q03 spec that asserts (1) `.fb-content` and `.ws-content` widths equal their `.panel` ancestor width within 1px and (2) the `.cw-input-line` bottom sits within 16px of `.cw-output`'s bottom edge (proves the prompt is anchored).
- Files changed: src/renderer/styles.css, tests/e2e/visual-polish.spec.ts.
- Typecheck: same 2 baseline octaveProcess.ts PATH errors only. Vitest: same baseline failures (octave-bundle + octave-plot integration), no new failures from CSS changes.
- **Learnings for future iterations:**
  - The "right-edge gray strip" inside dock panes was the default Chromium scrollbar gutter, NOT a layout/width bug. Chromium's default scrollbar track is light gray which only blends in the light theme. Always theme `::-webkit-scrollbar*` with the same vars used for thumb/track elsewhere — the project already has `--scrollbar-thumb` / `--scrollbar-thumb-hover` defined per theme; use them.
  - To anchor a child to the bottom of a flex column scroll container WITHOUT breaking scroll-into-view for overflowing content, use `display: flex; flex-direction: column` on the parent and `margin-top: auto` on the child you want anchored. When content fits, the auto margin pushes the child down; when content overflows, the auto margin collapses to 0 and content scrolls naturally.
  - `.cw-output` already auto-scrolls to bottom on new entries via a useEffect in CommandWindow.tsx, so the flex layout above doesn't conflict with the existing scroll-on-new-output behavior.
  - rc-dock's `.dock-content` does NOT paint a background. Children must paint their own bg explicitly (via `--bg-panel`) — if a content area is transparent, scrollbar gutter / sub-pixel gaps will leak through as gray.
---

## 2026-04-11 - US-Q04
- Discovered the root cause of the "every tab looks active" symptom: rc-dock embeds rc-tabs with `prefixCls="dock"` (verified at node_modules/.vite/deps/rc-dock.js:16264 — `React95.createElement(es_default9, { prefixCls: "dock", ... })`). The actual rendered tab classes are `.dock-tab`, `.dock-tab-active`, `.dock-tab-btn`, `.dock-tabpane`, `.dock-nav`, `.dock-ink-bar` — NOT `.rc-tabs-*`. Every `.rc-tabs-*` rule added by US-P09 was dead code.
- Added `.dock-tab` selectors in src/renderer/rc-dock-theme.css covering: idle (`--bg-header`/`--text-secondary`), `:hover` (`--bg-hover`/`--text-primary`), and `.dock-tab.dock-tab-active` (`--bg-panel`/`--text-bright`/inset accent box-shadow). Also added `.dock-nav`, `.dock-nav-wrap`, `.dock-nav-list`, `.dock-tab-btn`, `.dock-ink-bar`, `.dock-tabpane` to cover the rest of the rc-tabs-via-dock-prefix surface. Kept the legacy `.rc-tabs-*` rules as a defensive fallback per the AC wording.
- Added tests/e2e/dock-active-tab.spec.ts: pre-seeds config.json with a fixture layout that has commandWindow + commandHistory in the same pane, boots the app, walks from `[data-testid="dock-tab-title-matslop-command-window"]` up to its `.dock-tab` ancestor, enumerates sibling `.dock-tab` elements, and asserts exactly one carries `dock-tab-active` and an inset box-shadow while the others have neither.
- Files changed: src/renderer/rc-dock-theme.css, tests/e2e/dock-active-tab.spec.ts (new).
- Typecheck: same 2 baseline octaveProcess.ts PATH errors only. Vitest dock-layout + theme-default suites: 30/30 green.
- **Learnings for future iterations:**
  - rc-dock 4.0.0-alpha.2 calls `<Tabs prefixCls="dock" ... />` in DockTabs.render. So inside rc-dock, the rc-tabs internal classes are `dock-tab`, `dock-tab-active`, `dock-tab-btn`, `dock-tab-with-remove`, `dock-tab-disabled`, `dock-tab-focus`, `dock-tabpane`, `dock-tabpane-active`, `dock-nav`, `dock-nav-wrap`, `dock-nav-list`, `dock-nav-add`, `dock-nav-more`, `dock-nav-operations`, `dock-ink-bar`, `dock-extra-content`, `dock-dropdown*`. Theming the `rc-tabs-*` namespace inside MatSlop is a no-op — always use the `dock-` prefixed names.
  - Yet ANOTHER class collision: rc-dock ALSO has `.dock-content` (its panel content area) which is independent of rc-tabs' `.dock-tabpane`. The visual-polish.spec "tab fills its parent" assertion walks up to `.dock-content` — that's the rc-dock pane, not the rc-tabs tabpane.
  - To force a multi-tab pane in an e2e test, pre-seed config.json with `panelVisibility.commandHistory: true` AND a `dockLayout.dockbox` whose bottom panel `tabs` array lists both `matslop-command-window` and `matslop-command-history`. The sanitizer will NOT strip them because both are now visible.
---

## Codebase Patterns (additions)
- rc-dock embeds rc-tabs with `prefixCls="dock"`. Style the `.dock-tab*`, `.dock-nav*`, `.dock-ink-bar`, `.dock-tabpane*` selectors — NOT `.rc-tabs-*`. Verified via `grep -n 'prefixCls' node_modules/.vite/deps/rc-dock.js`.
- In rc-dock 4.0.0-alpha.2, `.dock-content` is the rc-dock pane content area; `.dock-tabpane` is the rc-tabs (prefixed) inner tab body. They are different elements — both exist in the rendered DOM.

## 2026-04-11 - US-Q05
- Forced `.dock-bar` background to `--bg-header` in all rc-dock panel styles (.dock-style-main, .dock-style-card, .dock-style-headless) by adding selectors with the same specificity as the upstream rules in node_modules/rc-dock/dist/rc-dock.css. Without these, `.dock-panel.dock-style-main .dock-bar { background: none }` (upstream) won out and the bar inherited `--bg-panel` from .dock-panel — leaving a visible color jump where the last tab ended.
- Themed `.dock-nav-wrap`, `.dock-nav-list`, `.dock-nav-operations`, `.dock-nav-more`, `.dock-extra-content`, and the rc-tabs equivalents (`.rc-tabs-nav-operations`, `.rc-tabs-nav-more`, `.rc-tabs-nav-wrap-ping-*`) to `--bg-header` so the area to the right of the last tab is one continuous strip.
- Killed the inset edge shadows rc-dock paints via `.dock-nav-more::after` / `.rc-tabs-nav-wrap-ping-*::before/::after` (used as overflow-fade indicators in the upstream light theme; show as dark seams on bg-header).
- Files changed: src/renderer/rc-dock-theme.css.
- Typecheck: same baseline (octaveProcess.ts PATH errors) — no new errors. Unit tests: same baseline (octave-config bundled-binary). No new failures.
- **Learnings for future iterations:**
  - rc-dock's upstream stylesheet has per-panel-style overrides (`.dock-panel.dock-style-main .dock-bar`, `.dock-panel.dock-style-card .dock-bar`) that are MORE specific than a bare `.dock-bar` selector. When theming dock chrome, always match that specificity (or prefix with `.dock-panel.dock-style-*`) — otherwise main/card panels silently fall through to upstream backgrounds.
  - The "filler" right of the last tab is composed of `.dock-nav-wrap` (the scroll viewport for tabs) plus `.dock-nav-operations` (overflow + extra-content area). Both must be themed for the strip to look continuous.
  - rc-dock paints inset shadows on overflow scroll indicators (`.dock-nav-more::after`, `.rc-tabs-nav-wrap-ping-*::before/::after`). They render as dark vertical seams against a dark theme — null them out with `box-shadow: none !important`.
---

## 2026-04-11 - US-Q06
- Added `minWidth: 180` to the right column (vertical box) AND to the workspace `PanelData` in `buildDockLayoutFromVisibility` so the workspace pane cannot collapse to a width that hard-clips the "No variables in workspace" placeholder.
- Updated `.placeholder-text` in styles.css with `padding: 8px 12px; margin: 0; white-space: normal; overflow-wrap: anywhere; word-break: break-word` so long placeholder strings wrap inside narrow panes instead of hard-clipping.
- `.ws-content` already had `overflow: auto` from US-P08; verified.
- Added unit test asserting both right-column box and workspace pane declare `minWidth ≥ 180`. All 28 dock-layout tests pass.
- Files changed: src/renderer/panels/MatslopDockLayout.tsx, src/renderer/styles.css, tests/unit/dock-layout.test.ts.
- **Learnings for future iterations:**
  - rc-dock IS now in node_modules (the previous "rc-dock not installed" baseline note in Codebase Patterns is stale — dock-layout unit tests now run cleanly, 28 passing). The only remaining baseline typecheck errors are `octaveProcess.ts(51)` PATH-typing issues, unrelated to renderer/polish.
  - rc-dock supports `minWidth`/`minHeight` on every box and panel via `DockDataBase` (see node_modules/rc-dock/lib/DockData.d.ts:81-84). Both `BoxData` and `PanelData` extend it through `BoxChild`, so you can set `minWidth` on either the column box or the leaf pane — set on both to defend against drag-resize squeezing the parent box around the pane.
  - rc-dock's `Divider.minSize` is computed from the children's `minWidth`/`minHeight`, so once a child declares minWidth the user physically cannot drag the divider past it.
---

## 2026-04-11 - US-Q07
- Added LAYOUT_SCHEMA_VERSION = 2 constant (bumped from implicit 1) and a `version?: number` field on LayoutConfig in src/main/appConfig.ts.
- New pure helper `migrateLayoutConfig(raw)` discards `dockLayout` when `raw.version` is missing or older than the current constant, preserves panelVisibility + panelSizes, and stamps the current version. Defaults applied to malformed/null input.
- `getLayoutConfig()` now runs the migration on every read AND persists the migrated config back to the store the first time it sees a stale layout (so the bump only fires once). `setLayoutConfig()` always stamps the current version.
- New unit suite tests/unit/layout-schema-version.test.ts (6 tests) covers: stale-version drops dockLayout, current-version passes through, malformed input → defaults, getLayoutConfig rewrites stale store entry, setLayoutConfig stamps version. All passing.
- Files changed: src/main/appConfig.ts, tests/unit/layout-schema-version.test.ts.
- **Learnings for future iterations:**
  - Layout persistence is in `electron-store` (key `'layout'`) via src/main/appConfig.ts — NOT in session.json (sessionStore.ts handles editor tabs only). When changing default layout shape that would be visually masked by an old persisted dockLayout, bump LAYOUT_SCHEMA_VERSION.
  - The renderer-side App.tsx already runs sanitizeSavedDockLayout on `layoutGet`, so a `dockLayout: undefined` returned by main is the cleanest "use defaults" signal — no extra renderer plumbing required.
  - electron-store mock pattern from tests/unit/theme-default.test.ts is the template: define a per-test `storeData` object captured in a closure and mock the default export with a class that reads/writes that object. `beforeEach` clears it.
  - `migrateLayoutConfig` is exported PURELY (no electron / fs imports inside the function) so it stays vitest-importable; keep all new appConfig helpers that way.
---

## 2026-04-11 - US-Q08
- Ran `npm run build` (clean) and then `npx playwright test visual-polish` against the polish-v2 branch with all US-Q01..Q07 fixes integrated. All 3 specs in tests/e2e/visual-polish.spec.ts passed: light-theme leak scan, US-Q03 fill assertions, dock-tab fills parent .dock-content.
- Generated/committed the dark-theme baseline screenshot tests/e2e/__screenshots__/visual-polish/default-layout-1600x1000.png (the spec writes the baseline on first run when missing; since the file was previously untracked, this run produces the canonical polished baseline).
- Unit tests (tests/unit/dock-layout.test.ts, 28 cases) still green. Pre-existing typecheck error in src/main/octaveProcess.ts is unchanged baseline.
- Files changed: scripts/ralph/prd.json (US-Q08 passes:true), tests/e2e/__screenshots__/visual-polish/default-layout-1600x1000.png (new baseline).
- **Learnings for future iterations:**
  - The visual-polish spec auto-creates the baseline PNG when missing (`if (!fs.existsSync(BASELINE_PATH)) fs.writeFileSync(...)`) — to refresh the baseline after a deliberate visual change, delete the file and re-run the spec.
  - `dist/renderer/` may be stale relative to the current source tree; **always** `npm run build` before running e2e specs in this repo or you'll be testing yesterday's bundle.
  - The polish-v2 cycle (US-P01..P10 + US-Q01..Q08) is complete; visual-polish is the cycle exit gate and is now green.
---
